# PROGRESS

> 這份文件只做一件事：**交接**。它記「現在在哪裡、什麼還沒解決、下一步做什麼」。
> 規格與決議在 [`docs/design.md`](docs/design.md)（v0.42），實作經過在 `git log`
> （每個 commit 的訊息就是那一輪的長版理由）。**兩邊已經有的東西，這裡不重複。**

最後更新：2026-09-04

## 0. 名字

**這個專案叫 Blockyard**（v0.42 改名，原本叫 Blocky Workflow）。理由兩個：
`Blocky` 與 Google 的 **Blockly** 差一個字母而且是同一個領域，遲早會被誤認；
而且 **`blocky` 在 PyPI 上本來就被別人佔了**——那個名字連 `pip install` 都做不到。

`Blockyard` = 積木場（材料堆在那裡，而你在那裡把東西組起來）。掃過 PyPI／npm／
GitHub 三邊都乾淨。**改名挑在這個時候是因為它現在幾乎沒有代價**：還沒有使用者、
還沒推上 GitHub，而 opcode 不含專案名（`http.get`），所以 §13.1 那條沒有被踩到。
**真正的死線是 `from blockyard import block`**——那是每個積木包的第一行，
有第二個人寫包之後就改不動了。

repo 的目錄名還是 `blocky-workflow`（改它會動到所有人的本機路徑，另外處理）。

## 1. 現況

**P0、P1、P2 全部結案。P3 — 擴散進行中**（第 1 步：擴充功能面板；**第 2 步：從電腦
匯入 `.zip`，本輪完成**）。**P3 的驗收句這一輪第一次跑得完**：把一個包壓成 `.zip`、
從面板匯入、拉出它的積木跑起來，全程不碰檔案總管與終端機。

```
cd backend && .venv/bin/python -m pytest      # 1131 passed, 5 skipped
cd packages/editor && npm run check           # 552 passed（30 檔）+ tsc 乾淨
```

> **改了 `extensions/` 就要跑不帶參數的 `pytest`**（`testpaths` 同時收 `tests`
> 與 `../extensions`）。只跑 `pytest tests` 會漏掉包自己的測試，而那些測試會
> import `main.py`——一個語法錯誤要等到瀏覽器裡按下按鈕才會出現。

> **要一個可以拿來試匯入的 `.zip`**，最快的做法是拿一個現成的包：
> `cd extensions && zip -qr /tmp/demo.zip demo -x "*/__pycache__/*" "*/.venv/*"`。
> 它的 id 已經裝著，所以你會看到「已經裝過了」那條路（Q24）；要走完整條安裝，
> 把解出來的 `manifest.yaml` 換一個 id 與目錄名再壓一次。**同 id 的第二次匯入
> 這一版是擋下來的**，而且刪除不刪磁碟——裝錯了要去檔案總管，見 §2 的「匯入」。

題庫覆蓋 **57/95** 顆內建積木（60%）。`tests/gen_corpus.py --check` 只驗不寫檔，
不帶參數會重新產生 fixture 與黃金軌跡。

積木包五個：`demo`(13) `discord`(3) `http`(4) `openai`(2) `panel`(5)。

## 2. 下一步

### P2 為什麼算結案

驗收句（設定每天 09:00 的流程、關掉瀏覽器、隔天檢查執行歷史）在 2026-09-02 實測
通過。範圍表裡最後那一格「`try_catch` 的多個 catch」**決議不做**（D30）：要分辨
「這個錯誤歸哪一個」就用 `${錯誤.code}` 進 `如果⋯否則如果⋯`——那個機制已經在了，
而且**它本來就是 catch 鏈的語意**（由上往下、第一個成立的就停）。

### P3 — 擴散（§15，v0.38 重排）

**範圍收成一句話：積木包怎麼進來、怎麼出去。** 擴充功能面板（D31）→ 從電腦匯入
`.zip` → 匯出 Bundle（§10.1）與分享。**AI 生成積木包與 Tauri 打包移到 P4**：生成
出來的東西要有地方去（裝、看、拔掉），而那條路就是這一階段的產出——先做生成等於
先做一個沒有出口的東西。

**驗收**：把一個積木包壓成 `.zip`，在另一台機器上從面板匯入、拉出它的積木跑起來，
全程不碰檔案總管與終端機。

| # | 步驟 | 狀態 |
|---|---|---|
| 1 | **擴充功能面板**（D31）：整頁的卡片牆、搜尋、加入／移除 | **完成** |
| 2 | **從電腦匯入 `.zip`**：解壓、驗 manifest、venv、§12.1 的審閱確認 | **完成** |
| 2b | **積木包的一生**：搬家、收據、解除安裝、更新（§16 Q24） | **下一步** |
| 3 | **匯出 Bundle 與分享** | 撞 §16 Q3 |

**下一步是把擴充功能這條路做完**（第 2b 步），不是往下一個主題走。設計已經寫完
並且拍板：[`docs/extension-design.md`](docs/extension-design.md)，**全部未實作**，
那份文件最後一節是施工順序。摘要：

1. **工具箱那顆 bug**（刪掉目前選著的分類 → 先切回第一格）——是 bug 不是設計題
2. **擴充功能的家搬到 `~/.blockyard/extensions/`** ← **唯一有時效的一項**：現在是
   一個 `mv`，有使用者之後是在別人機器上做資料遷移
3. **來源收據**（`.blockyard-source.json`，官方那幾個補發 `origin: "official"`）
4. 解除安裝 ＋ 垃圾桶 → 5. 更新（Q24 結案）→ 6. GitHub URL → 7. 登記處

**第 1 步做完之後，手邊多了什麼、還缺什麼**：

- 面板在工具箱左下角那顆 `＋`。卡片上的圖案是從 manifest 長出來的（色底是分類色、
  中間是名字的第一個字），所以新的包不必附圖也長得對。
- **「加進來」是工具箱的可見性，不是安裝**（D31）。註冊照舊全部註冊——舊專案載得
  進來、畫布上的積木照樣跑。名單存 localStorage，載入專案時與 IR 的 `extensions`
  （算出來的，§13.3）**聯集**，所以打開別人的專案不會出現「畫布上有積木、工具箱裡
  生不出它」。
- **左鍵加、右鍵刪，而且畫布上還有它的積木時不准刪**——滑到其中一顆、說還有幾顆、
  **再按一次刪除跳下一顆**（被擋住的人接下來要做的事就是一顆一顆刪掉，而畫面上一次
  只看得到一顆），與「還有 N 個地方在呼叫這個函式」是同一條路（`blocksUsing()` +
  `glideToBlock`）。**右鍵的主要入口是分類欄上那顆色圓點**（面板上的卡片接同一份
  選單）；那一格問得回自己是哪一個包，靠的是工具箱 JSON 的 `toolboxitemid`。這條規則同時讓上面那條聯集不自相矛盾：不擋的話，刪掉一個正在
  用的包，下次開專案它又自己回來。**刪除不刪磁碟上的東西**——卡片還在，回到
  「＋ 加入」。
- **右鍵選單的第二條「更新／替換」是暗的，只寫進了文件**（§16 Q24）：三個要先回答
  的情況是「新版少了一顆積木」（畫布上那顆退化成佔位符）、「參數變了」（就是 Q21，
  只是發生在按下更新的那一刻）、「版本要不要釘住」（IR 已經記了 version，沒有人
  讀它）。傾向是**更新前先算差集、交叉畫布上真的用到的那幾顆，攤出來再確認**——
  與刪除同一條線：會讓積木失去來源的動作都要先說出誰會受影響。
- **右上角那顆 `＋ 從電腦(.zip)` 本輪接上了**，見下一節。

### 匯入：一個包從外面進來（P3 第 2 步，本輪完成）

**入口是擴充功能面板右上角那顆按鈕。** 選一個 `.zip` → 審閱畫面 → 按下安裝 →
它出現在工具箱的分類欄上。這一輪之前，「裝一個包」的唯一做法是把資料夾複製到
`extensions/` 再重啟後端——那不是一個功能，是開發者的工作流。

**兩段式，而中間那一步是使用者**：`POST /api/extensions/import` 解到
`~/.blockyard/import-staging/<token>/` 並回一份審閱資料（**還沒裝**），
`POST .../import/{token}` 才真的裝。分兩段不是介面好看——§12.1 那句「安裝前完整
顯示原始碼，不可略過」**只有在審閱的是真的會被裝進去的那份 bytes 時才成立**，而
一次呼叫的做法會逼前端上傳兩次同一個檔案，第二次可以是別的檔案。

**解壓那一關自己走每一條規則**（`extensions/install.py`），不用 `extractall()`：

- **symlink 拒收**。zip 存得下它，而一條指向 `~/.ssh/id_rsa` 的連結每個字元都合法。
- **解開之後的總量**（128MB）才是擋 zip bomb 的那條——壓縮前的大小永遠很小。而且
  加總不能只信 zip header 報的 `file_size`（那是壓縮檔說的話），寫的時候要邊寫邊數。
- **最外層那一層資料夾要砍掉**。對著資料夾按右鍵壓縮得到的是 `greet/manifest.yaml`
  ——**那是使用者最可能做的動作**，它要成立，而不是回一句「找不到 manifest.yaml」。
  砍的是「最外層那一層」不是「叫這個名字的那一層」（`greet 複本/` 也要過）。
- **macOS 的 `__MACOSX/` 與 `.DS_Store` 跳過，不是拒絕**。為了兩個使用者看不見的
  檔案說「這個 zip 有問題」，等於要求他去學 `zip -x`。順序也有意義：**先跳過再算
  最外層字首**，反過來就砍不掉了。
- `..`、絕對路徑、`C:\` 一律拒絕而**不是清洗後放行**——把 `../../x` 悄悄改成 `x`
  會讓一個惡意的 zip 裝進來之後看起來完全正常。

**§12.1 的靜態掃描落地了**（`extensions/codescan.py`），而它的定位要寫清楚：
**它不是一道門**（`getattr(__builtins__, "ev" + "al")` 一行就繞過去，而積木包本來
就以使用者權限跑在他自己的機器上），**它是一支指著程式碼的手指**——審閱畫面上攤開
的是幾百行 Python，這裡做的事是把「第 42 行在開子行程」放到他眼前。所以它**不擋
安裝**，寧可誤報也要說，而**文案不能說「安全」**。命中的 import 與呼叫對回
`permissions`，沒宣告的排在審閱畫面最前面（§12.1 表格裡的「與宣告不符時警告」）。

**審閱畫面的形狀由那句「不可略過」決定**：右邊一整欄從一開始就是 `main.py`，不是
一顆「檢視程式碼」的按鈕（**一個沒有人按的按鈕與沒有那份程式碼是一樣的**）；而
「安裝」在最上面，**不在原始碼捲完之後**——要捲到底才按得到是一種假的儀式，它換來
的不是「使用者讀完了」，是「使用者學會了怎麼快速捲到底」。左邊那一欄是他讀得懂的
東西（權限、依賴、會多出哪幾顆積木、要填哪幾把金鑰、`open_url` 會開到哪裡去）。

**順序是先建 venv 再搬進 `extensions/`。** `requirements` 的下載是這條路上唯一真的
會失敗的一步（網路、沒裝 `uv`、版本解不開），而它只吃 id 與 `requirements`，不在乎
檔案在哪。先搬的話失敗留下的是「裝在那裡、拉出來卻跑不動」；先建的話失敗留下的是
什麼都沒有。

**一個包讀不進來不再讓其他包跟著消失。** `discover()` 原本一份讀不過就整批拋，於是
一份手滑的 manifest 讓 `GET /api/extensions` 回 500、編輯器畫出「連不上後端」——那句
話指錯主詞，而且它把使用者手上**全部**的積木一起帶走。現在是 `scan()`：跳過壞的、
收進 `problems`，由 `GET /api/extensions/problems` 端到擴充功能面板上。**跳過的代價
是一個包安靜地消失**（對正在寫包的人是最難查的症狀），所以錯誤不能被丟掉——它要在
那個包本來會出現的地方說話。

**同 id 的第二次匯入這一版擋下來**（§16 Q24），訊息指名「更新／替換還沒接上」而不是
說它壞了。連帶一個現在就存在的坑：**裝錯一個包沒有路可以拔掉它**——D31 的右鍵刪除
刻意只收工具箱、不刪磁碟，所以要碰檔案總管。那與 Q24 是同一題的兩半。

**設計寫成一份獨立文件**：[`docs/extension-design.md`](docs/extension-design.md)
（三層帳、來源收據、更新的差集規則、GitHub 與登記處那條路）。下面這幾條的長版
理由都在那裡。

**還沒做的**：

1. **更新／替換（Q24）**，見上。它是「裝得進來就會有第二次裝」的必然。
2. **從磁碟移除一個使用者裝進來的包**。它與 D31 的「刪除只收工具箱」是兩條不同的
   刪除，選單上會有兩個都叫「刪除」的東西——那個分界要在 UI 上說清楚才做得下去。
3. **`testpaths` 收不到 repo 外面的包**（3.1 已記）。匯入進來的包住在 `extensions/`
   底下，所以現在還成立；`.zip` 開始從別的地方來時就不成立了。
4. **審閱畫面沒有鍵盤走訪的實測**（3.5 的老問題：焦點真的停在哪只有瀏覽器答得出來）。

**實測時踩到的第一件事不是匯入本身，是「後端沒重啟」**：新的路由在 `create_app()`
建 app 時註冊，所以改完後端沒重啟時那個端點根本不存在——而 `packages/editor/dist`
存在時，後端 `/` 底下掛著 `StaticFiles`（只收 GET/HEAD），一個打不中 API 的 POST
就掉進那個 catch-all，回 `Method Not Allowed`。畫在畫面上聽起來像「這個端點不收
POST」，真正的意思是「這個端點不存在」。現在 `inspectExtensionZip` 對 404／405
換成一句說得出下一步的話（`client.test.ts` 釘住那條界線：**其他狀態碼照舊用後端
那一句**，不然「這個 .zip 裡沒有 manifest.yaml」會被一個猜測蓋掉）。design.md
§14.1 的表也多了這一列。

### 面板：積木包畫自己的畫面（§16 Q17，v0.40 決議 B 路線）

**決議：積木包給的是 HTML/CSS/JS，不是資料。** 早期草稿傾向 A（宣告式 widget、
封頂的字彙表），本輪先做了 A 再整條拆掉——理由只有一句：**A 的代價是每加一種
圖表就要改編輯器一次**，而一個想畫 three.js 的包永遠等不到那一天。

`extensions/panel/` 就是它的第一個消費者，整包是示範：`main.py` 每顆積木一行
`ctx.send_panel(...)`，畫圖的是 `ui/`（canvas 折線圖、平移縮放、自動適應）。
**編輯器不知道什麼是折線圖。**

機制（全部有測試）：

- **`panels:` manifest 宣告**（`PanelSpec`）。分頁是**宣告出來的**，不是資料生
  出來的——早期版本用標題當身分，而標題可以插值，一個迴圈就能生出無限多格。
  `entry` 必填、只收 `.html`、路徑規則擋在載入期，解析後再驗一次（字串規則擋
  不住 symlink）。
- **`GET /api/extensions/{id}/asset/{path}`**——「積木包可以把 bytes 交給瀏覽器」
  的唯一入口。副檔名白名單決定 content-type（不從內容猜）、`nosniff`、只有宣告
  過 `panels` 的包有這條路。
- **`sandbox="allow-scripts"` 的 iframe**（**刻意不給 `allow-same-origin`**）→
  opaque origin：碰不到 `parent.document`、`localStorage` throw、`fetch` 帶
  `Origin: null`。而子資源照常載入，所以 vendored three.js 那種多檔案成立。
- **`ctx.send_panel(panel_id, payload)`**：host **一個字都不解讀** payload，只驗
  **收件人**（`panelId` 要是這個包宣告過的，`panels_of` 由註冊表注入）。**兩邊
  各驗一次**——那條路是 notification，host 端的錯誤傳不回來。
- **編輯器留這次的訊息記錄，`ready` 之後重播**。iframe 換分頁或搬進彈出視窗一定
  會重載（規格），重播讓那件事不痛，也順便解掉「面板還沒開就先送」。

**面板跨 Run 累積，`begin()` 不清它。** 理由是 §5.1 的「點一下就跑」：每次執行
都清的話，點一顆 `加一個點` 只會看到一個點，那顆積木等於不能單獨點。清空由使用者
拉一顆 `清空圖表`。**這條讓兩本帳對齊**——先前只清了編輯器那一半，於是切一下分頁
畫面就會少掉前幾次的東西。

**還沒做的**（照該做的順序）：

1. **快照標記**（協定加一格，例如 `replaceAll: true`）。訊息記錄有上限（2000），
   而**截斷之後重播是錯的**——被丟掉的那幾則裡如果有一個 `clear`，重播出來的圖會
   多出一段本來已經清掉的東西。跨 Run 累積讓這件事從「一萬圈才遇到」變成「跑幾十
   次就會遇到」。`清空圖表` 天生就是快照，第一個該標。
2. **`ext.panel` 的流量控制**。它是現在唯一完全不受 §6.2 管的事件——一個
   `重複 10000 次` 就是一萬則獨立事件過 WebSocket。**而且沒辦法照舊的方式修**：
   舊的收斂懂 payload 的語意，B 路線的前提是我們不懂。能做的是**批次**（同窗口同
   面板合成一個陣列），省的是 frame 數不是事件數——**這是 B 路線真實的代價**。
   `broker.py::collapse` 的 docstring 現在還在描述三條**已經不存在**的規則。
3. **雙向通道**。`ExtPanel` 有 `onMessage` 但沒有人傳，`@panel` 的 `on_message`
   設計了沒實作——所以協定是**單向的**，面板上一顆「匯出 CSV」的按鈕什麼都做不了。
   它與已知缺口「`@button` 的 `action: call`」是同一條路，先做那個等於做一半。
4. **theme token**。面板長得不像編輯器，`ui/panel.css` 裡的顏色是**抄過去的常數**
   ——換配色時每個包要各自跟著改，而它們不會。
5. **payload 大小上限**。`block.exit` 有 4KB 截斷，`ext.panel` 沒有。不是安全問題
   （包本來就跑在你機器上），是一個手滑會讓編輯器卡住而畫面上不說為什麼。
6. **§12.1 的審閱畫面要有 `panels` 那一列**（新權限：這個包會在編輯器裡顯示自己
   的畫面）。那個畫面整個還不存在。

### 已知缺口（照咬人的頻率排）

1. **變數面板在迴圈結束後還留著迴圈變數。** `for_each` 與 `count_to` 仍然發
   `var.set`，但那個名字現在是第 2 層的——迴圈跑完面板上還寫著 `item = 2`，而
   `取得 (item)` 已經是錯誤。**面板說的話與執行期不一致。** 便宜的解要 `var.set`
   帶一個「哪一層」，而那是一次 schema 決定。**`count_to` 讓它更常見了**：計數
   迴圈比「對每一項」平常得多。
2. **`concurrency` 的 `queue` 與 `restart` 沒實作**（§5.1），目前與 `parallel` 同
   行為。`drop` 做了，因為 `when_cron` 宣告的就是它。
3. **cron 沒有 interval**，只有五欄 crontab，所以最小粒度是一分鐘（§9.1 說兩種都要）。
4. **編輯器沒有時區輔助**：拖出一顆 cron 積木之後存檔一定先失敗一次。宣告刻意沒
   有 default（給 UTC 會讓台北的使用者在下午五點觸發），所以那是**對的行為**；
   真正的解是 `defaultFrom: timezone` 那類宣告，而那是一次 schema 決定。
5. **§6.3 的 Trace 模式沒做**（使用者明確開啟後全量落地）。它是那條篩選規則的
   逃生口，但目前沒有人被咬到需要它。
6. **重試沒有指數退避**。正確的形狀要連上限與 jitter 一起決定。
7. **`@button`（D25 的 `action: call`）整條沒實作**——白名單裡沒有 `button`，也
   沒有那個端點。`discord` 的「測試連線」是它最自然的第一個消費者，而**面板的
   雙向通道走同一條路**（見上面「面板」那節的第 3 項）。

> **原本的第 1 條「後端的積木集合變了，開著的分頁不會知道」本輪結案。**
> `App.tsx` 在視窗重新取得焦點時重問 `/api/extensions`，只重新註冊**變過的**那幾
> 個命名空間（`changedManifestIds`，純函數、有測試）。掛在 focus 上是因為那就是
> 那件事發生的形狀：改宣告 → 切到終端機重啟後端 → 切回瀏覽器。**畫布不動**——
> 只換工具箱、擴充功能面板與轉換 context；後端拿掉一顆積木時，畫布上那一顆要到
> 下次載入才退化成 §13.3 的佔位符（刻意的：沒人要求時動使用者的畫布更糟）。

## 3. 未解決問題與已知限制

只列還會咬人的。長版理由在 `git log` 與程式碼註解裡。

### 3.1 積木包與 Host（P1 留下的）

- **`http` / `openai` / `discord` 都沒有 conformance 題目**：§17 的題庫跑不了本地
  伺服器與外部帳號，所以那三條路的回歸網是各自的 `tests/`（`http` 那份含一題走
  引擎的端到端）。
- **`discord` 的測試靠蓋掉 `discord.http.Route.BASE`**。`openai` 有 `base_url`
  這條乾淨的路（自架相容端點是真的存在的東西，測試搭順風車）；Discord 沒有相容
  端點，所以一個 `base_url` 設定會是「為了測試而長在使用者面板上的一格」。代價是
  那份測試綁著 SDK 的一個內部名字，**換大版本時要複驗**。
- **`discord` 的 hat 仍然沒有參數**：「只聽某個頻道」很有用，但 hat 的參數要一路
  穿過 `start_trigger` 才到得了 `@trigger` 函式，而 §9.2 的 trigger 生命週期本來
  就是後面的事。現在塞進去等於在生命週期還沒有主人的時候先決定它怎麼變。
  **積木上那格 `message` 不是參數**（D32）：它從不離開 host，只決定 yield 綁成
  哪個名字，所以它也**不進 trigger 的 `spec`**——進去的話，改一個變數名就會把
  gateway 連線拆掉重接。
- **`discord.on_message` 的 yields 只有一個 `message` 物件**（D32，v0.39）：
  `${message.content}`、`${message.author.bot}`、`${message.url}`。名字由使用者
  在帽子上填。**既有專案裡的 `${content}` 會變成未知變數**，這是那條決議認下的
  破壞性代價。
- **`discord.on_message` 寫死濾掉自己那隻 bot 的訊息**。別的 bot 不濾
  （`${message.author.bot}` 交給畫布判斷）。這是全包唯一一條寫死的過濾，理由是
  「收到訊息就回一句」是這顆 hat 最直覺的第一個用法，而它會讓 bot 對著自己講到
  被限流。
- **`openai` 沒有 `count_tokens`**：`tiktoken` 第一次用會自己去
  `openaipublic.blob.core.windows.net` 抓 BPE 檔（用 `requests`，不經 `ctx.http`）。
  要補回來就得先決定「包可以自己連網嗎」，並把 `TIKTOKEN_CACHE_DIR` 釘到
  `~/.blockyard` 底下。**注意這一題現在有前例了**：`discord` 就是一個合法地自己連網
  的包（見 §7.4），所以答案已經不是「不行」，而是「守得住的是 venv 與審閱」。
- **`openai` 的 `chat_full` 沒有 `temperature`**：新的 reasoning 模型會不會拒收這個
  參數，不打真 API 驗不出來。**要加就得先拿真金鑰打一次**。
- **`openai` 與 `discord` 的測試用 `InProcessHost`**，所以兩個包都得裝在 backend 的
  dev extras 裡。副作用：`venv.py` 的 `.pth` 把 backend 的 site-packages 接進每一支
  積木包 venv，所以**沒有自己宣告 `requirements` 的包會吃到 backend 這一份**。兩個
  包都自己宣告了，現在不咬人。
- **`venv.py::_link_backend_site_packages()` 只在建立 venv 時跑一次**（`pyvenv.cfg`
  不存在才呼叫）。venv 在、`.pth` 卻不見了就不會補回去，症狀是子 process 連
  `blockyard` 都 import 不到。改成無條件寫就沒事（它是 idempotent）。
- **逐次的 timeout 不能設**：只有 host 那份 30 秒預設值。要做就是 `request` 多一個
  `number` 參數。
- **積木包自帶的 `tests/` 靠 `testpaths = ["tests", "../extensions"]` 收**。包來自
  repo 外面時這條就不成立了。**匯入進來的包剛好還在 `extensions/` 底下**（§14 說使用者
  裝的包住那裡），所以現在仍然成立——但它現在是靠「裝到哪」撐著，不是靠設計。另外它靠 `addopts = ["--import-mode=importlib"]` 才容得下
  每個包都叫 `tests/test_blocks.py`。
- **manifest 是 `palette` 一份清單**：三種條目（`opcode` / `button` / `section`），
  `blocks` 由模型導出。**加第四種條目時記得兩邊都要認**——後端
  `manifest.py::_entry_kind`、前端 `define.ts` 的三個 narrowing 函式，共用的是「有沒有
  那個 key」這條規則，而它沒有被抽成一份東西。
- **一個包的 manifest 壞掉，其他包照舊**（本輪修掉，見 §2 的「匯入」）。壞的那個走
  `GET /api/extensions/problems`，在擴充功能面板上說自己為什麼不見。**`main.py` 壞掉
  仍然不影響列表**（那條路只讀 manifest）；語法錯誤現在會在**匯入的審閱畫面**上被指名
  （`codescan.py`），但**已經裝好的包不會**——那要等使用者拖出那顆積木按下去。
- **`@button`（D25 的 `action: call`）整條沒實作**：`blockyard/__init__.py` 的白名單裡
  沒有 `button`，也沒有 `POST /api/extensions/{id}/button/{name}`。`discord` 的
  「測試連線」本來是它最自然的第一個消費者，這一輪用 `open_url` 的說明按鈕頂著。
- **`reads` 宣告沒有消費者**（`toolbox.ts::findVariableReader` 與後端欄位都留著）。
  下次動那塊時重新決定留或刪。

### 3.2 靜態檢查（`ir/checks.ts`）

- **「有路徑未回傳」是退化版**：只守「一顆 `回傳` 都沒有」，半條路沒回傳仍然只有執行期看得到。
- **參數積木放錯地方仍然只有執行期會說**，而且 D29 沒有改變這件事。它缺的不是
  走訪機制（現在有了）而是**時機**：從定義帽子上拖一顆參數出來，落點必然先在
  函式體外面，祖先鏈不會讓那一刻變得不一樣。同理**不該**做成存檔期錯誤——那會
  打破「寫到一半的積木要能存檔」。
- **檢查不進存檔路徑**（§8.5「不阻擋連接」），孤兒警告與 `CheckRunner` 是兩本帳。

### 3.3 編輯器

- **一顆積木都註冊不出來的積木包不會出現在擴充功能面板上。** 面板列的是分類
  （`groupByManifest` 的產物），而分類是從**註冊得出來的積木**長出來的——一個
  `palette` 全是 `dynamic` 或 `deprecated` 的包因此完全隱形。現在四個包都不是這樣，
  所以沒有人被咬到；P3 第 2 步（使用者自己塞包進來）之後這件事會變得可能。

- **積木包新增一個參數之後，既有專案裡那一格是空的輸入孔**（§16 Q21）。填不了東西、
  看不出該放什麼。**修在 `deserialize` 是錯的**——試過，會打破 3 題 roundtrip：
  `object.get` 的 `default` 孔**刻意可以不存在**（不寫 = 找不到 key 就報錯），所以
  「IR 裡沒這個孔」不等於「這參數是新加的」。IR 目前分不出這兩件事，而那正是 Q21
  要先回答的。
- **監聽中的 Run 走專案通道**（`/ws/project/{id}`，D33），不再輪詢。輪詢那條路
  有一個修不掉的洞：一則 Discord 訊息的 Run 只有零點幾毫秒，所以「問到再接」永遠
  追不上，而使用者看到的是一片安靜——他在帽子底下放的那顆 `記錄` 看起來沒有作用。
  現在是**在 Run 開始之前就接著**，變數、高亮、log 一起回來。**斷線不補、不留
  backlog**（理由見 D33），前端斷了 1 秒後自己重連。
- **右側面板改成分頁列**（`執行` + 每個啟用中的包宣告的面板）。變數與輸出**刻意
  留在同一頁**：除錯時最常見的動作是「看變數變成什麼，同時看 log 說了什麼」，
  拆成兩頁就是逼人二選一。
- **「點一下就跑」之後按鈕卡在「停止」——本輪修掉，是一個競態。** 監聽開著時
  「點一下就跑」不開 run 通道（D33），事件走專案通道；而一次 1 毫秒的 Run 比一趟
  HTTP 往返快得多，所以 `run.start` 與 `run.end` 常常在 `POST /api/runs` 回來
  **之前**就到齊了。`attach()` 無條件寫 `status: 'running'`，就把一個終局推回
  「執行中」，而那個 Run 不會再有任何事件來關掉它。**這是 D33 那條路上第二個
  「1 毫秒的 Run 比一趟往返快」**（第一個是 Discord 訊息追不上輪詢）。
- **標頭在錯誤訊息很長時會擠成兩行**（按鈕跟著換行）。資訊都在，只是難看。
- **`ui.multiline` 存不下「強制單行」**；`Shift+Enter 換行` 沒有提示；autocomplete 只補 root。
- **字面值型別切換不進 undo 堆疊**（`literals.test.ts` 有一條測試釘住這個行為，
  **它變好的那天會紅**）；切完會失去 manifest 的 `min` / `max`（存檔重載會回來）。
- **孔裡插著 reporter 時，底下那顆影子的值存不下來**（IR 的 `kind: block` 說不出它）。
  同一個縫讓 `ui.multiline` 在被蓋住的影子上也存不下來。
- **下拉欄位的值沒有載入期驗證**：`fields.op` 寫成 `"wat"` 的 IR 存得進去。
- **回傳型別只影響形狀與靜態檢查，不影響執行**；參數積木沒有型別的形狀提示（刻意，§8.5）。
- **`reshapeProcedure` 沒有 catch**：第七輪那個 bug 的症狀之所以是「東西不見了」就是
  例外漏出去。成因修好了，防護還沒加。
- **帽子的參數孔擋不住別的積木**：靠 listener 收拾，而那一瞬間丟進去的積木會被存檔
  丟掉，目前沒有提示。
- **鍵盤走不進浮動工具列與預覽積木**，所以分段的文字與順序只有滑鼠改得動。
- **四處依賴 Blockly 內部行為，換版時要複驗**：右鍵選單的 `preconditionFn` 快取、
  `WidgetDiv` 靠焦點活著（按鈕用 `pointerdown` + `preventDefault`）、keydown 必須
  capture 才問得到「編輯器開不開著」，以及 `motion.ts` 靠
  **`scroll(ws.scrollX, ws.scrollY)` 是不動點**（`scroll()` 夾完就直接寫回那兩個
  欄位）——`glideToBlock` 用「先讓 `centerOnBlock` 跳到定位、記下落點、再跳回起
  點」問出目的地，好處是不必抄一份 `centerOnBlock` 的公式，代價是這條性質。它不
  成立的症狀是**滑到的位置偏一點點**，不是壞掉。加上三個 CSS hack：`.injectionDiv
  { overflow: visible }`、欄位編輯器的 inline style（`!important`），以及把鍵盤
  導覽的 `--blockly-active-tree-color` 蓋成 `transparent`（那圈貼著工具箱與整張
  畫布邊界的藍框）——名字改掉的症狀是藍框回來，不是壞掉。

### 3.4 執行期與後端

- **「子行程意外結束」那句話原本會指錯主詞。** 回報的現場是一個**不回應的網站**
  （換一個網址就好了），而最可能的機制是：積木包卡在那個請求上 → 使用者按了停止
  或再按一次執行 → 那個 Run 收尾時 `unload` → `_kill` 砍掉還在等回應的子行程 →
  進行中的呼叫拿到 `PeerClosed`。**那不是積木包壞掉，是我們自己砍的**，而使用者
  做的動作是「停止」。現在先問「這個 worker 還是目前那一個嗎」，不是就說「還沒
  跑完，這次執行就結束了」。真正意外死掉的那條路也補了死因（`_why_gone`：結束碼
  ／被第幾號信號終止／行程還在只是連線斷了）——三種成因完全不同，而原本那句話
  三種長得一模一樣。**這條沒有測試**：它要的是「呼叫進行中把 worker 換掉」，
  而那是時序，不是規則。
- **點一下就跑 = 每次都先存檔**（PUT + POST），沒有節流。監聽也是（按下監聽會先存）。
- **`block.enter` 沒帶展開後的字串**，所以「滑過欄位看到實際送出的內容」還不存在。
- **錯誤紅框淡出之後 `error` 這個 phase 還在**：同一顆積木在同一次執行裡錯第二次不會
  重新倒數（要有 `try_catch` 才做得出來）。
- **`block.error` 沒有 traceback**，§8.3 的「點擊展開」目前只展得出 hint。
- **運算積木不發子步驟事件**（§4.7b）；`var.set` 的窗口內收斂（§6.2）比文件寫的多。
- **§5.5 的「清理有 5 秒上限」沒實作**，停止時只是 `cancel()`。
- **遞迴 headroom 是估的**（`PYTHON_FRAMES_PER_BLOCKYARD_FRAME = 24`），靠 `RecursionError` 兜底。
- **`blockyard serve` 沒有正式打包測試**：只驗過 `python -m blockyard.cli`。
- **偏好存 localStorage**，§16 Q15（偏好放哪）未定案。

### 3.5 測試缺口

- **審閱畫面（`ImportReview.tsx`）沒有元件測試**：規則那一半抽成純函式測了
  （`importRules.ts`：宣告與掃描併表、沒宣告的排最前面、`eval` 不算「不符」），
  剩下的是版面與焦點——同下面那條。
- **版面與時間只有瀏覽器實測守著**（jsdom 量不到 `getBoundingClientRect` /
  `getComputedTextLength`）：擴充功能面板的卡片牆（`gallery-grid` 的欄數、卡片
  比例、標頭那三欄的置中）、半形單字置中、值氣泡的 hover 凍結、淡出的時間、浮動工具列
  的位置、flyout 版面、focus trap「Tab 真的停在哪」，以及 `motion.ts` 的兩段動畫
  （flyout 捲動、`glideToBlock`）——`easeOut` 是純函式測得到，「滑到的是不是那顆
  積木」不是。
- **動態下拉的「依賴那一格變了就重抓」那條 workspace listener 沒有單元測試**——它要
  一個真的 workspace。`load()` 那一半有測（args 進 body、快取 key 含 args、值不會被
  清掉），瀏覽器裡也實測過選伺服器 → 頻道清單跟著換。
- **「建立一個新函式」那條路沒有單元測試**（`placeDefinition` 要畫面）。改簽章那條有。
- **`data.list_insert` 用 `len+1` 正規化索引，無測試**，可疑。
- **§17.2 有幾列還寫不出題目**：`concurrency` 的 drop/queue/restart、`CancelledError`
  穿透、§6.3 的落地——都要等 P2 的機制存在。
- **題庫跑的是 `load()`，不是 `api/validation.py`**，所以 cron / webhook / Q19
  份數那三個驗證在題庫裡寫不出來（D29 的存檔期檢查因此刻意放進 `load()`）。
  這條界線沒有人守著：新增一條存檔期規則時，「放哪裡」決定了它有沒有題目。
- **hat 的 `yields` 穿過函式呼叫那條規則題庫寫不出來**：`Case` 沒有
  trigger／payload，題目永遠用綠旗跑，所以 layer 0 一定是空的。那條靠
  `tests/unit/test_bindings.py`。
- **那 45 顆沒有題目的積木**，參數名有 AST 測試守著，缺的是「它真的跑得動」那半。

## 4. 未決題

design.md §16 的 **Q1、Q3–Q5、Q7–Q9、Q11、Q12、Q14、Q15、Q20、Q21、Q23、Q24、Q25** 仍未決。

**Q25（檔案在這個系統裡是什麼）本輪新增，排進 P4。** §4.3 的六種值裡沒有檔案，
所以一個處理 mp4／PDF／Excel 的積木包會同時撞上三件事：產出的檔案沒有家（沒有
「這次 Run 的工作目錄」）、產出的檔案到不了瀏覽器（asset 端點只吐包目錄裡的東西）、
以及**使用者指不出一個檔案**（瀏覽器的 `<input type="file">` 給的是 bytes 不是路徑）。
建議是**路徑版先做、`file` 值型別不做**；排 P4 是因為真正的系統檔案對話框要 Tauri。

已決的：Q17（積木包畫自己的面板，**走 B：自帶 HTML/CSS/JS**，本輪）、
Q2（廣播訊息，不做，D14）、Q6（函式暫存變數 `這次`，做，v0.33）、
Q10（開發者路線，v0.17）、Q13（內建宣告放後端，D21）、Q16（字面值型別，第 6 步）、
Q18（`control.stop` 是 cap block，第五輪）、Q19（可重複參數群組，做，v0.29；
第二個消費者「多個 catch」不做，D30）、Q22（webhook 密鑰進 keyring，v0.28）。

**三題現在到期，各缺一個決定：**

- **Q24（積木包更新／替換的語意）。現在是 P3 唯一擋著的那一題。** 匯入做完了，所以
  「能裝第一次就會有第二次」不再是預測——同 id 的第二次匯入現在會撞上一句「還沒接
  上」。要先答的那一題仍然是「新版少了一顆積木」該擋還是該警告後放行（傾向：**那幾顆
  在畫布上才擋**，與 D31 的刪除同一條線）。**順帶要一起答的是「裝錯了怎麼拔掉」**：
  D31 的刪除刻意不刪磁碟，所以現在裝錯一個包只能去檔案總管——而那正好是 P3 驗收句
  說「全程不碰」的那個東西。

- **Q20（`照字典序比` 要不要留）。** 判準是「P1 結束時如果沒有任何一份真實專案或
  題庫選過 `text`，就在那時拿掉」。**數過了**：唯一用到 `mode: "text"` 的是
  `operator/ordering_text_mode_is_lexicographic` ——**那一題是為了測這個模式而寫
  的**，不是有人需要它。照判準的字面意思這叫沒有使用者；但「一個功能自己的測試
  算不算證據」是循環的，所以最後那一下要人來按。拿掉的話是 §13.1 的
  deprecated（宣告留著、工具箱隱藏、既有專案照跑），不是刪 opcode。
- **Q21（積木包新增一個參數之後，既有專案裡那一格是空的輸入孔）。** 見 3.3——
  修在 `deserialize` 已經試過是錯的，IR 目前分不出「這個孔是新加的」與
  「這個孔刻意可以不存在」（`object.get` 的 `default`）。

---

## 5. 從實測裡學到、值得帶到下一輪的事

> 每一條都是**真的踩到**才寫下來的，不是通則。

1. **「規則對了，但少走了一條路。」** 每一輪幾乎都是這個形狀，而共同點是每次都省了
   一步「使用者真的會怎麼做」，**那一步只有把東西交出去才問得到**。
2. **單元測試每一步都綠，串起來才炸。** 修法是讓那幾步變成一個函式，不是再抄一份
   順序去測。
3. **規則對、時機錯。** 加檢查時要問的不只是「它說的對不對」，還有**「它會在哪一刻說」**。
   兩個實例：動態下拉的依賴變了「只重抓、不清空這一格的值」（清空要判斷這次變動是
   不是使用者造成的，而載入專案／undo／拖動走的是同一條事件路）；以及「參數積木
   放錯地方」那條檢查**至今沒做**，缺的不是機制而是時機（3.2）。
4. **測得到的是規則，測不到的是順序。** 凡是「我讀的狀態別人也在改」的判斷，都要問
   一次「我跑在誰前面」。
5. **兩邊都對，中間那句話沒有人負責。** **把東西端到端跑一次是唯一問得出這種問題的
   方法**——而它花的時間比寫那兩份測試少。
6. **驗收驗了值，bug 在樣子。** 寫驗收句時要問：**這句話如果只有一半壞掉，我看得出來
   嗎。** 中過兩次，而且兩次都只有瀏覽器看得到：`default: ""` 的下拉畫出來是
   一個沒有文字、沒有箭頭、看起來不能點的空膠囊；新增參數之後既有專案那一格是一個
   填不了東西的空孔。**兩件事的單元測試都是綠的，因為值一直都是對的。**
7. **一份宣告要真的到得了前端才算數。** Q19 的 `repeat` 在後端、在 vitest 裡
   全都對，瀏覽器裡卻完全沒有那兩顆按鈕——因為 `/api/extensions` 是
   `exclude_defaults=True`，而那天測的是一個**改宣告之前就啟動**的後端。同一
   輪還踩到第二次（webhook 的 `/hook` 預設值也是舊的）。教訓不是「記得重啟」，
   是**「端到端」要包含宣告那條路**：後端物件對、TS 型別對、前端邏輯對，中間
   那個 HTTP 回應仍然可能少東西，而症狀是「什麼都沒發生」。
8. **一條規則的兩半分開走，其中一半會安靜地不存在。** v0.23 判了「`設定 [參數名]`
   是存檔期錯誤」，文件寫得很清楚，而**程式碼裡從來沒有過**——全部測試綠著，因為
   沒有人為一條不存在的規則寫測試。D29 要動同一條規則時才翻出來。教訓不是「要記
   得實作」，是**決議與題目要同一次交出去**：§17.2 的表格如果那時就多一列，這件事
   當天就會被發現。

9. **「這一題如果機制壞掉，它會紅嗎」要真的把機制拆掉試一次。** Q6 的遞迴題原本
   寫成 `回傳 (前一項 + 費氏(n-2))`——看起來完全對，而它用舊的 `設定` 也會過，
   因為 §4.6 的求值順序由左而右，`前一項` 在內層那次呼叫蓋掉它**之前**就讀完了。
   一題永遠是綠的測試比沒有測試更糟：它會讓人以為那條規則有人守著。

10. **「這條規則套在別的積木上會怎樣」要自己先問一遍。** Q6 交出去的第一版寫死
    「只准放在函式體內」，而使用者第一句話就是「hat 底下也可以嗎」——跑一次就是
    `A 數到 5 / B 數到 8`。**答案 D29 早就寫好了**（範圍 = 綁它那顆積木的 body，
    hat 的 body 是整條腳本），我只是沒把那句話套到新積木上。這是第 1 條的變形：
    少走的那條路不一定是「使用者怎麼操作」，也可能是**「這條規則的另一個外層」**。

11. **「沒有人用到它」是一個可以查的事實，不是一個可以假設的前提。** 搬 opcode
    時我寫了「沒有任何專案存過它」——查一下就知道是錯的（使用者五分鐘前才拖出來
    試過，編輯器自動存了）。§13.1「opcode 永不移除」擋的就是這個。**一條規則存在
    的地方，就是別人已經踩過的地方。**

12. **一個動作跨兩本帳時，undo 只會退回其中一本。** 刪掉一個函式同時動了 Blockly
    的工作區（自己管 undo）與 React 的 state（不進 undo 堆疊），於是 Ctrl+Z 之後
    畫布上留著一顆帽子、而那個函式已經不存在——**兩邊各自都是對的**，錯的是中間
    沒有人負責（第 5 條的形狀，只是這次那條縫在自己家裡）。修法是讓兩本帳其中
    一本**跟著**另一本走，而且問的要是「現在的狀態是什麼」而不是「剛剛發生了
    什麼」：前者的 redo 免費就對了，後者要再寫一次反向邏輯。**下次遇到「undo
    之後怪怪的」，先數這個動作動了幾本帳。**

13. **序列化存得下的東西比你以為的少。** 同一個 bug 的第二半：`deletable` 存得
    進 `blocks.save()`，**拖曳策略存不進去**——所以 undo 還原出來的參數晶片看起來
    完全正常（型別對、位置對、刪不掉），拖一下卻是把它從帽子上扯下來而不是複製
    一份。凡是「我們逐顆掛上去的」東西（drag strategy、listener、警告），都要問
    一次「這顆積木被還原時誰把它掛回去」。

14. **翻裝下來的原始碼，不要用訓練資料裡的記憶去猜。** `discord` 那一輪三件事都是這樣翻出來的
   （`login()` 不開 gateway、`ctx.http` 注不進去、`jump_url` 吐 `@me`），三件如果照
   猜的寫下去都會是「到執行期才炸、而且訊息指錯方向」。上一輪的 `httpx2` 也是。
   **這條規矩到目前為止的命中率是 100%。**

15. **用字串位置切檔案，錨點必須是唯一的。** 清理死掉的 CSS 時，終點錨點用的是
    一行**通用的分隔註解**（`/* ---- *`），而它在檔案裡出現很多次——`indexOf`
    比對到更後面那一個，中間 **1493 行**全被吃掉：Blockly 主題、擴充功能面板、
    執行歷史、autocomplete、欄位樣式，全沒了。同一輪對 `store.ts` 做同樣的事，
    切掉了 `case 'log'`、`applyProject` 與 `finish`。
    **差別在誰在守**：`store.ts` 那三個當場被測試抓到，CSS 沒有測試，所以它一路
    撐到使用者打開瀏覽器說「怎麼變那麼醜」。改檔案要用唯一比對，不要用位置。

16. **瀏覽器的限制要一條一條驗，而每一條的症狀都不一樣。** 這一輪 sandbox iframe
    連著撞了三次，三次都**看起來完全不像 CSP**：
    ① `frame-ancestors` 寫成後端的 origin，而內嵌它的是編輯器那一頁（dev 下是
    Vite 的 5173）→ 畫面顯示「localhost 拒絕連線」，看起來像伺服器沒開。
    ② `default-src` 同一個錯，只是被 ① 擋在後面還沒發作。**根因是一句話：後端
    算不出瀏覽器用的是哪個 origin**（它看到的是被代理之後的自己），所以那個值只能
    由前端 `?embed=` 帶上來——而它原樣進 header，是注入的入口，要驗。
    ③ `<script type="module">` 的抓取**一律走 CORS**（classic script 不會），而
    opaque origin 拿自己的檔案也算跨來源 → 症狀是「HTML 與 CSS 都到了、JS 沒跑」，
    因為 `<link rel=stylesheet>` 是 no-cors。
    順帶：`'self'` 在 opaque origin 下匹配不到自己的子資源，CSP 要寫明確的 host。

17. **靜靜地什麼都沒發生，要有一個時限說話。** 上面 ③ 的第一個症狀是一格顯示
    正常、但永遠不會動的面板——「JS 沒跑起來」與「還沒收到訊息」在畫面上一模一樣。
    現在等 3 秒沒收到 `ready` 就浮一句話，而且**收件人是寫那個包的人**：它說得出
    下一步去哪裡看（devtools 選那個 frame）。這是第 6 條的延伸：**這件事如果只有
    一半壞掉，我看得出來嗎**——而「完全沒反應」是最不容易看出來的那一半。

18. **先做一半再拆掉，比一開始就做對便宜。** Q17 這一輪先做了 A（編輯器畫的
    widget、封頂字彙表），交出去之後才看清 A 的代價是**每加一種圖表就要改編輯器
    一次**，然後整條拆掉換成 B。那些被刪掉的東西（`PANEL_LIMIT`、被擠掉的計數、
    橫向捲的分頁列）**全部是在替一個錯的模型止血**——「分頁由資料生出來」一換成
    「分頁由宣告生出來」，三個補丁一起消失。
    值得記的不是「A 是錯的」，是**補丁在替哪一個假設止血，那個假設才是要換的東西**。

19. **一條「整批拋」的規則，在使用者自己塞東西進來的那天會變成另一種東西。**
    `discover()` 一份 manifest 讀不過就整批拋，這件事被記在 PROGRESS 裡很久了，
    而它一直只是「開發時偶爾撞到」——因為 `extensions/` 底下的每一個包都是我自己
    放的。P3 第 2 步把「誰放的」換成使用者之後，同一行程式碼的症狀從「我改壞了」
    變成「這個工具連不上後端，而我全部的積木都不見了」。**判斷一條錯誤處理夠不夠
    好，要問的不是「它對不對」，是「誰會踩到它、那時候他手上有什麼」。**
    而修法的另一半同樣重要：跳過壞的很容易，難的是**跳過之後那個包安靜地消失**——
    所以錯誤要在那個包本來會出現的地方（擴充功能面板）說話，不是寫進 log。

20. **一份「不可略過」的東西，做成一顆按鈕就等於略過了。** §12.1 的第一列寫的是
    「安裝前完整顯示原始碼，不可略過」，而最自然的實作是一顆「檢視程式碼」的展開
    按鈕——那句話的字面意思完全滿足，實際上沒有人會按。同一輪的另一半是相反方向
    的誘惑：把「安裝」放在原始碼捲完之後，看起來很負責，換來的其實是**使用者學會
    怎麼快速捲到底**。**規格說「使用者必須看見 X」時，要問的是「畫面上什麼都不做
    的那個人會看見 X 嗎」**，而不是「X 有沒有一條路可以到達」。
