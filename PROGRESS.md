# PROGRESS

最後更新：2026-08-29（第 6 步：`FieldText`、§16 Q16 字面值型別、真／假積木）｜ 12 commits
｜ 後端 `cd backend && .venv/bin/python -m pytest` → 611 passed, 3 skipped（題庫 74 題）
｜ 前端 `cd packages/editor && npm test` → 224 passed（75 highlight + 63 round-trip + 33 FieldText
+ 25 define + 17 literals + 11 run store）

> 第 5c 步（運算積木）沒有單獨 commit，與第 6 步合併在同一筆裡。第 1b 節保留它的
> 說明。

## 1. 本次完成

**P0b 第 6 步：`FieldText`（§8.5）**——設計文件把它標成「整個 P0b 唯一的高風險項」，
理由是 Blockly 的自訂 field API 在富渲染上很難纏。做法照 §8.5 與 §15 連續兩處的
警告：**一個類別、用 options 開關能力**，不是三個 field 之後再合併。

這一步同時收掉第二輪實測回饋的第三件事——**「文字型態的輸入框比數字型態大一號」**。

### 尺寸：文字欄位比數字欄位大一號

根因不在樣式，在 `initView`。Blockly 的 `FieldInput.initView()` 有一段
`@blockly/field-multilineinput` 沒有抄到的規則：

```js
this.isFullBlockField() || this.createBorderRect_();      // 影子不畫外框
if (this.isFullBlockField()) this.clickTarget_ = block.getSvgRoot();
```

zelos 的 `FULL_BLOCK_FIELDS` 打開時，**「整顆積木只有這一格」的簡單 reporter**
（也就是字面值影子）不畫欄位外框——影子本身就是那顆白色膠囊（Scratch 的字面值
格子），再畫一層等於在膠囊裡放一個方角的白盒子。多出來的正好是外框自己的
左右各 8px 內距與 4px 圓角，於是同一句話在文字影子裡比在數字影子裡寬 16px、方
4px。plugin 的 `initView()` 無條件畫，所以每一格文字都大一號。

補回那條規則之後，`(0) + (0)` 的數字橢圓與 `( ) = ( )` 的文字橢圓量起來一樣；
非影子上的文字格（`設定 [d] 為`、`運算 [(1+2)*3]`）則畫成與數字同高的**膠囊**
而不是方角盒子。尺寸公式也逐行對齊 `Field.updateSize_`——差一項，兩種影子在畫面
上就是不一樣大，而使用者一眼看得出來。

**`clickTarget_` 那一半不能省**：影子上的空欄位只畫得出一個 NBSP，沒有任何字形，
而 SVG 的命中測試打不到沒有幾何的東西。少了它，空的文字格子變成點不開的——這個
症狀只在**空值**時出現，是最容易漏測的那種。

### `${}` pill、整格取值、運算式紅線

`render_` / `updateSize_` 整組換掉：plugin 是一行一顆 `<text>`，而 pill 要的是
「一行之內分段」。新的流程是「`ir/highlight.ts` 把字串切成 run → 依序量寬 →
排版 → 回頭補底圖」。

- **`${路徑}` 畫成 pill**（靛色），**整格取值**（§4.7 的 `whole`）換一個色系
  （綠色）並填滿整格——它說的是「這一格回傳原值，可能是物件或清單」，換色系
  而不是換深淺，深淺會被讀成「同一件事的程度差別」。
- **語法錯誤畫紅線**：`${a+b}`、`${` 沒關、運算式的 `2 +`，字轉紅 + 底線，
  外框轉紅，積木上掛 warning icon。**存檔仍然由後端的 422 擋**，紅線只是提早說。
- **運算式用等寬字**，編輯中的 textarea 也是——進出編輯狀態時字形跳一下比一路
  不等寬更難讀。

### autocomplete（`$` 觸發、變數名稱聚焦即列）

popup 掛在 **Blockly 的 `WidgetDiv` 底下**，不是 `document.body`：那個 div 已經
被定位在欄位上、會隨畫布捲動與縮放一起移動，而 `WidgetDiv.hide()` 會清空它——
於是「編輯器關了 popup 還留在畫面上」那類 bug 從一開始就不存在。這是 §8.5 點名
的難題（「autocomplete popup 在 textarea 裡怎麼定位」），答案是不要自己定位。

鍵盤處理**掛在 `document` 的 capture 階段**：plugin 的 `onHtmlInputKeyDown_` 綁在
textarea 自己身上，而同一個元素上 capture 與 bubble 的先後是註冊順序決定的，
掛在 document 才穩定地跑在它前面——Enter 才有機會先被 autocomplete 收走而不是
直接提交欄位。

候選**只來自變數名稱欄位**，不含 `${}` 裡打過的 root。§4.5 的靜態檢查就是這麼
定義的（一個名字要先被 `data.set` 過才算數），把引用也收進來等於讓 autocomplete
幫忙傳播錯字。路徑第二段（`${a.` 之後）不補——那要知道值長什麼樣子，是執行期的事。

### 右鍵選單

`ContextMenuRegistry` 的 scope 只給得出**積木**，給不出**欄位**。但
`preconditionFn` 拿得到開啟選單的原始事件，而 precondition 一定跑在 displayText
與 callback 之前——所以在那裡把事件的 target 對回欄位、存起來給後兩者用。

- **「重新命名「x」的所有引用」**（§4.5）：一次掃全工作區，名稱欄位與 `${}` 引用
  一起換，包在同一個 Blockly event group 裡（設計文件明講要**一次 undo**）。
  只動路徑的 root，`${舊.items[1]}` → `${新.items[1]}`。
- **「多行輸入」**（§8.5 第 3 層）：接上 `blocks[].ui.multiline` 的讀寫。
  manifest 已經宣告成多行時 disabled——那個 key 是一份名單，講不出「不要多行」。

**編輯器不是另一種外觀。** 點一格欄位，編輯器是一個 HTML `<textarea>`，圓角、
對齊、寬度全部由 Blockly 與 plugin 自己決定，跟底下那顆膠囊沒有關係。三個症狀
都來自這件事，也都由同一條規則收掉（`resizeEditor_` → `syncEditorShape`）：

- **點下去當場變方框**——`<textarea>` 的圓角寫死成 `BORDERRADIUS`。改成單行畫
  膠囊、多行畫圓角矩形，而值一直在變（每個按鍵都 `setValue`），所以打了換行會
  變方、刪回一行會變圓，都是即時的。
- **點下去當場變寬**——plugin 的 `EDITOR_MIN_WIDTH` 是 150px，於是點一格短欄位
  會看到它撐開一大截、放開又縮回去。那個下限現在**只有多行才套**：進入多行本來
  就是一次形狀變化，寬度跟著變不突兀；單行只是想改幾個字。
- **左邊 padding 看起來太小**——單行欄位有最小寬度（`MIN_PILL_ASPECT`，不然一個
  字的膠囊會變成直立的橢圓），而靠左排版把多出來的寬度**全部留在右邊**。改成
  **單行置中、多行靠左**：置中把那份餘裕平均分到兩側，而一段多行文字置中是沒
  辦法讀的。SVG 與 `<textarea>` 兩邊用同一個判斷，不然點下去文字會左右跳一下。

**plugin 的快捷鍵提示條關掉了**（`FieldMultilineInput.showHint = false`）。它是
一條絕對定位在編輯器底部的橫幅，每一格文字都得替它多留一行高度與一段寬度，
而它說的事（Enter 送出、Shift+Enter 換行）是按一次就知道的。常駐的教學橫幅
換來每一格都變大，不划算。

**超過 `rows` 之後要捲到游標那一行。** plugin 的 Shift+Enter 是自己接的：直接
改 `input.value` 再設 `selectionStart`，而**程式設定選取範圍不會讓瀏覽器把游標
捲進視野**（只有真正的鍵盤輸入才會）。症狀是打到第五行之後畫面停在前四行。
算術抽成純函數 `caretScrollTop()`——沒有畫面就試不出來的東西，而它錯了只有真的
打到第五行才看得到。

**畫幾行由值決定，形狀由畫幾行決定。** 中間試過一條「多行欄位至少畫 2 行」，
想讓「多行輸入」有立即的視覺回饋——結果是 `記錄 (你好)` 這種 manifest 宣告了
`multiline: true` 的欄位，明明只有一行字卻被畫成兩行高的**矩形**，第三輪回饋
立刻指出來。那條拿掉了：宣告與右鍵開關管的是「這一格容不容得下換行」
（`maxLines`），不是「它現在多高」。代價是空欄位上按「多行輸入」看不出變化，
由選單文字翻成「取消多行輸入」來說。

### §16 Q16：非字串字面值（決了，也做了）

`data.set`、`operator.eq` 這類參數在 manifest 裡宣告成通用的 `type: string`
——那句話說的是**「這個孔用文字框編輯」**，不是「這裡只能是字串」。但編輯器
只給得出文字影子，於是打 `99` 存出去是 `"99"`，布林與 `null` 根本沒有入口。
`operator.eq` 想測「`"5"` 不等於 `5`」時，兩顆字面值被同一份宣告套成同一種
影子，量出的差異反而消失。

**沒有採用**設計文件的候選 (c)（manifest 上補 `literalKinds`）：那是把猜測寫進
宣告——沒有人知道 `data.set` 的 `value`「應該」允許哪幾種，答案是「全部」，
而全部就等於不用宣告。

**採用候選 (a)**：影子的型別跟著**值**走，再給一個右鍵切換。

- 影子從兩種變成四種：`text` / `number` / `boolean` / `null`。布林用**下拉**
  （`真` / `假`）而不是 checkbox——白色影子裡的一個勾勾看不出「沒勾 = false」
  還是「這格是別的東西」，而兩個字直接說出目前的值，順帶它自己就是切換介面。
  `null` 只有一個標籤，所以那個右鍵選單**不能只掛在 `FieldText` 上**。
- 右鍵「改成文字 / 數字 / 是非 / 空值」，目前是哪一種就不列那一項。
- **切換保留看得懂的值**，規則對齊 §4.3 的轉換表：`"12"` → 12、`真` → `"true"`、
  `null` → `""`。唯一與後端不同的是「轉不動」——後端報錯，這裡退回 0，因為
  使用者只是在選單上點了一下。
- **讀的那一半一行都沒改**：`deserialize.ts::buildShadowState` 從第 4 步就寫著
  「宣告與值的型別不一致時一律信任值本身」，所以來回自動是通的。這是第 4 步留
  的著力點真的被用到的一次。

### 真 / 假：兩顆布林字面值積木

六角形孔**沒有影子**（§8.1 明講那是刻意的：塞一顆預設的 `false` 進去會讓
「還沒填」與「填了 false」看起來一模一樣）。於是「我就是要一個常數 false」在
畫布上原本沒有出口，只能繞 `不成立 (空孔)`，而那顆積木讀起來像在問問題。
`operator.true` / `operator.false` 兩顆沒有參數的積木把那條路補上，題庫 +1 題。

### 第二輪體感回饋的另外兩件

| 症狀 | 根因 | 修法 |
|---|---|---|
| **多行欄位是膠囊型** | 影子沒有外框，畫的是**積木自己的路徑**，而 zelos 依 `block.getOutputShape()` 決定畫膠囊還是圓角矩形 | 畫出來超過一行時把影子的 output shape 設成 `SQUARE`（半徑 = `CORNER_RADIUS`），單行維持 `ROUND`。改欄位的 `rx` 沒有用——膠囊的兩端仍然會露在外面。判斷依據是**渲染出來的行數**而不是 `isMultiline()`：宣告成多行、值只有一行的欄位仍然是膠囊 |
| **變數名稱欄位扁掉，像 `border-radius: 50%`** | 膠囊的圓角是 `rx = 高/2`，而 SVG 會把 `rx` **夾到寬的一半**；`設定 [d] 為` 的名字只有一個字，寬度撐不到高度，於是變成直立的橢圓 | 有外框的單行欄位給一個最小寬度（`1.6 × 高`），短名字也是一顆躺著的膠囊 |

第二條只套在**有外框**的欄位上。字面值影子沒有外框，它的寬度得跟數字影子一致
——那正是這一步前半剛對齊好的東西。

### 一句話不能有兩套：`field_messages.yaml`

前端要在打字的當下畫紅線，就必須有一份**不會丟例外**的解析器（後端遇到第一個
錯誤就 raise，而畫面上「錯誤之前那幾段」仍然要畫得出來）。那是第二份實作，
風險是漂移——欄位裡看到一句話、按存檔看到另一句話，使用者會以為那是兩個問題。

處理方式是 **`backend/tests/fixtures/field_messages.yaml` 由兩邊各讀一次**
（`test_editor_mirror.py` 與 `highlight.test.ts`），加上一條用 AST 掃
`template.py` / `expression.py` 所有 `ValidationError` 訊息的覆蓋測試：**新增一條
文法規則卻沒進 fixture 就會紅**。前端複製一份測資自己維護是刻意避開的做法——那樣
漂移的那天兩邊都會繼續綠著，這與題庫 fixture 由前端直接讀後端 yaml 是同一個理由。

界線也寫在 `highlight.ts` 開頭：**這裡不是規格，後端才是**；存檔路徑
（`ir/template.ts` 的 `hasInterpolation` / `isWholeTemplate`）刻意不吃這個檔案，
存檔的正確性不該依賴顯示層的錯誤復原。

### 測試

- `highlight.test.ts`（75 個）：共用 fixture 的 45 條、run 的切法、重新命名、
  以及與存檔契約的交叉比對。
- `FieldText.test.ts`（23 個）：autocomplete 什麼時候跳、候選排序、掃描工作區、
  重新命名掃得乾不乾淨、`ui.multiline` 的**完整來回**（工作區 → serialize →
  loadProject → 再讀欄位）。只測一半的話，存得進去卻讀不回來會安靜地通過。
- `literals.test.ts`（17 個）：影子的型別跟著值走、`coerceLiteral` 的轉換表、
  以及**切完存得出對的 JSON 型別**（走完整來回——只驗畫面上那顆影子換掉了等於
  沒驗，Q16 的重點就是存出去的是 99 不是 `"99"`）。
- `test_editor_mirror.py`（48 個）：後端對同一份 fixture 的答案。
- 題庫 +1 題（`operator/boolean_literals`）：真 / 假插得進六角形孔。

**端到端實測**：`運算 (${d} * 2 + )` 在欄位裡把 `+` 標紅，按執行之後後端回的
422 是**同一句話**——「運算式在這裡就結束了，少了一個數字或變數」。

## 1b. 同一筆 commit 裡的第 5c 步

**運算積木（D23、§4.7b）**——回饋原話是「數學運算全靠拉積木很容易造成積木肥大」。
做法是給 §4.7 那句「請改用積木」一個**去處**，而不是把 D9 那條線往下移：

- **線沒有動**。D9 守的是**字串欄位**；字串欄位裡的 `${a+b}` 仍然是存檔期錯誤，
  只是訊息現在指向運算積木。
- **一顆積木、一個欄位、一個型別**：`operator.expr` 是 reporter、回 number，
  唯一的參數是 `type: expression`。它是 field 不是輸入孔——插一顆 reporter 進去
  就把它蓋掉了，同一個值兩個來源。
- **文法收死**：`+ - * / %`、括號、單目負號、數字、`${路徑}`。界線由 tokenizer
  擋，所以「下一步就是三元運算子」不會偷偷發生。
- **運算元與 §4.7 共用同一個 parser**（`template.parse_path` / `resolve_ref`）；
  **`/` 與 `%` 與那兩顆積木共用一份實作**（`ir/values.divide` / `modulo`）。
- **解析在存檔期**，語法錯誤是帶 `blockId` 的 422。
- 兩個體感 bug 也在那一步收掉：放大畫布時積木面板跟著變寬（`getFlyoutScale()`
  固定回 `DEFAULT_SCALE`）、`＋積木包` 按鈕蓋住最後一個分類（縮成 1.6rem 方形
  圖示 + 捲動容器補 `padding-bottom`）。

## 2. 未解決問題與已知限制

### 這一步留下的

- **前端有第二份解析器**（`ir/highlight.ts`）。它是顯示層、後端才是規格，兩邊
  由 `field_messages.yaml` 與那條 AST 覆蓋測試釘住——但**釘的是訊息，不是語意**。
  例如「哪些字元算運算」若只改一邊而兩邊的訊息剛好沒變，測試不會叫。
- **「重新命名」的輸入框是 `window.prompt`**（`Blockly.dialog.prompt` 的預設）。
  能用，但它是原生 modal，樣式與其他地方對不上。要換就是 `Blockly.dialog.setPrompt`
  接一個自己的對話框。
- **`ui.multiline` 存不下「強制單行」**。§4.2 定義的是一份「要多行的名稱清單」，
  講不出「不要」。所以右鍵只提供「多行 ↔ 自動」兩態；貼上一段長字串後想把它擠回
  一行，目前沒有出口。
- **autocomplete 只補 root**。`${a.` 之後不補——那要知道值長什麼樣子。真的要做
  就得靠上一次 Run 的 `block.exit` 值，那是 §6.3 落地之後的事。
- **`block.enter` 沒帶展開後的字串**，所以 §8.5 說的「滑過欄位可看到實際送出的
  內容」還不存在。這條要動後端的事件 payload（`engine.py` 的兩處 emit）。
- **`rows` 只是捲動上限，不是高度**。宣告 `rows: 3` 的 `解析 JSON` 在值只有一行時
  就畫一行——這是第三輪回饋確認過想要的行為，但它讓 `rows` 這個宣告在畫面上
  幾乎看不出作用（只有值超過那麼多行時才生效）。
- **空欄位上按「多行輸入」看不出變化**。它改的是 `maxLines`，而高度跟著值走。
  唯一的回饋是選單文字翻成「取消多行輸入」。
- **`Shift+Enter 換行` 現在沒有任何提示**（提示條關掉了）。這是刻意的——但它
  也表示「多行怎麼打」變成一件要自己發現的事。第一次用的人可能只會按 Enter
  然後以為打不出換行。
- **裝著多行欄位的那顆 reporter 仍然是膠囊**（`檢視 (四行文字)` 外面那圈）。
  zelos 的 reporter 一律畫成 dynamic ROUNDED，與欄位的形狀規則是兩回事；要改
  得動父積木的 output shape，而那會連帶影響它插進哪種孔。沒有人抱怨之前先不動。
- **每次值變動都重跑一次分析並呼叫 `setWarningText`**。字串都是幾十個字元，單機
  沒有痛感；但它與「落單堆疊的警告每次工作區事件都全掃一遍」是同一類的隱憂，
  §8.5 的靜態檢查（第 7 步）如果也這樣接，三個加起來就要重想。
- **右鍵選單靠 `preconditionFn` 把「被點到的欄位」快取起來**給 displayText 與
  callback 用。它依賴「precondition 一定先跑」這條 Blockly 的內部順序，而那不是
  公開契約——Blockly 換版時這裡要複驗。
- **pill 沒有 hover 提示**。滑到 `${resp.items[1]}` 上不會告訴你「resp 還沒有被
  設定過」；那是 §4.5 靜態檢查的事（第 7 步）。
- **字面值的型別切換不進 undo 堆疊**。Blockly 把「孔上的影子」當成父積木狀態的
  一部分，`createShadowBlock` 明寫 `recordUndo: false`。`literals.test.ts` 有一條
  測試把這個行為釘住，**它變好的那天會紅**——屆時把那條改成「一次 undo 退得
  回去」，而不是有人默默以為它一直都能 undo。
- **切完型別會失去 manifest 宣告的 spinner 上下界**（`min` / `max`）。存檔重新
  載入就會回來（`buildShadowState` 在型別一致時挑宣告的那顆影子），所以它自己
  會修好——但在那之前那一格是通用數字框。
- **`真` / `假` 沒有進 §17.2 以外的題目**。題庫有一題（插得進六角形孔），但
  「布林字面值 + `type.of`」「存檔來回」這些邊界還是靠前端的單元測試守。

### 沿用下來的

- **點一下就跑 = 每次都先存檔**（PUT + POST）。沒有節流，快速連點就是好幾次存檔
  加好幾個 Run。單機沒有痛感，接上遠端後端時要重看。
- **落單堆疊的警告每次工作區事件都全掃一遍頂層積木**。
- **`var.set` 的窗口內收斂**（同名只留最後一次，§6.2）比設計文件原本寫的多。
  之後要做「變數變化的歷史曲線」時，這條收斂是第一個要重新談的東西。
- **§6.3 的 SQLite 落地沒做**，執行歷史只在記憶體、上限 50 個 Run，重啟就沒了。
  `GET /api/runs/{id}/events`（附錄 A）因此還不存在。`persist_*` 同樣跨 Run 但
  **不跨後端重啟**——D12 明講要有的性質，目前是欠的。
- **§5.5 的「清理有 5 秒上限」沒實作**。停止時只是 `cancel()`。
- **`block.error` 沒有 traceback**：`BlockyError.to_dict()` 只有 type/code/
  message/blockId/hint，所以 §8.3 說的「點擊展開 traceback」目前只展得出 hint。
- **變數面板的開關存 localStorage**，§16 Q15（偏好放哪）仍未定案。
- **P1 剩下的部分刻意延後**：SubprocessHost 與跨 process 的反向通道（§7.6）、
  `ctx.http`（§7.4）、secret 值遮蔽（§12.2）、migrations（§13.2）。
- **§8.4 完成了轉換，但沒有互動編輯**：函式的 mutator 對話框、變形失敗後的孤兒
  處理、靜態警告全部還是第 7 步的事。
- **變數索引 `variables` 目前一律存空物件**（§4.5 說它是衍生索引）。變數監看面板
  的來源是執行中的 `var.set` 事件；**autocomplete 的來源則是掃描工作區**（§8.5），
  兩者都不吃這個索引，所以它繼續留空仍然是誠實的。
- **動態下拉（`source`）還是文字框**。要 `POST /api/extensions/{id}/dropdown/{source}`
  （§8.1 第 4 步）。
- **題庫覆蓋 43/88（49%）**。那 45 顆沒有題目的積木，**參數名有 AST 測試守著**，
  缺的是「它真的跑得動」那半。`data.list_insert` 用 `len+1` 正規化索引，仍**無
  測試**，可疑。
- **運算積木不發子步驟的事件**（§4.7b）。`block.exit` 只有最後那個值。
- **積木面板的縮放與畫布脫鉤之後，面板就沒有辦法放大了**。要獨立的面板縮放就得
  另外給一個控制項。
- **§17.2 有幾列還寫不出題目**：`concurrency` 的 drop/queue/restart、
  `CancelledError` 穿透、§6.3 的 SQLite 落地——都要等 P2 的機制存在。
- **`blocky serve` 沒有正式打包測試**：`[project.scripts]` 加了，但只驗過
  `python -m blocky.cli`，沒驗過 `pip install` 之後的 `blocky` 指令。
- **遞迴 headroom 是估的**：`PYTHON_FRAMES_PER_BLOCKY_FRAME = 24`
  （`interpreter/engine.py`）為經驗值，靠 `RecursionError` 保險絲兜底。
- **Q10 目標使用者未定**（教育 vs 開發者）。不阻擋 P0b；P1 的三個手寫包開工前必須定。
- 設計文件 §16 的 Q1、Q3–Q9、Q11、Q12、Q14、Q15 仍未決（**Q16 這一步決了**，見 §1）。

## 3. 下一次的第一個 TODO

**P0b 第 7 步：函式 mutator、形狀重塑與孤兒處理、靜態警告（§8.5）**（估 0.5～1 週）
——完成驗收 2、3、4。三件事都要用到這一步留下的東西：

- **靜態警告**（`${x}` 的 x 沒有被 `data.set` 過、型別不符的插孔）掛的是同一套
  warning icon 機制，而「這一格引用了哪些變數」現在有現成的 `referencedRoots()`，
  「工作區裡有哪些變數」有現成的 `collectVariableNames()`——兩邊湊起來就是 §4.5
  那條檢查。
- **形狀重塑的孤兒**要標 warning，與這一步的欄位警告共用 id 命名規則
  （`inert.ts` 開頭那段「帶 id 清除」的說明是唯一要遵守的約束）。
- **函式分類的動態工具箱**要跟著 `procedures.ts` 走，那份「每函式一組類型」的
  過渡機制到第 7 步才換掉。

§16 剩下的未決題（Q1、Q3–Q9、Q11、Q12、Q14、Q15）都不阻擋第 7 步。**Q10（目標
使用者是教育還是開發者）在 P1 的三個手寫積木包開工前必須定**——那三個包要寫成
什麼樣子，答案完全取決於它。

**時程**：P0b 仍在 **5～7 週**這條線上；第 6 步是估的 1～1.5 週裡最不確定的一項，
而它沒有超出。
