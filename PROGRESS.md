# PROGRESS

> 這份文件只做一件事：**交接**。它記「現在在哪裡、什麼還沒解決、下一步做什麼」。
> 規格與決議在 [`docs/design.md`](docs/design.md)（v0.13），實作經過在 `git log`。
> 兩邊已經有的東西，這裡不重複。

最後更新：2026-08-30

## 1. 現況

**P0b 第 5c～7 步全部做完**，含八輪瀏覽器實測回饋。編輯器現在有：`FieldText`
（`${}` pill、autocomplete、多行、右鍵選單）、字面值型別切換、運算積木、
`≈` / `≉`、執行前的三條靜態檢查、`建立一個積木` 對話框與 D26 簽章模板、
`procedure.param` 參數積木、形狀重塑與孤兒處理、cap block（`回傳` / `control.stop`）。

```
cd backend && .venv/bin/python -m pytest      # 645 passed, 5 skipped（題庫 84 題）
cd packages/editor && npm test                # 309 passed（11 檔）
cd packages/editor && npx tsc --noEmit        # 乾淨
```

題庫覆蓋 43/88 顆積木（49%）。

## 2. 未解決問題與已知限制

### 2.1 靜態檢查（`ir/checks.ts`）

- **「有路徑未回傳」是退化版**。§4.6 要的是真的算每一條路徑，現在守的是最有把握
  的那一格：**一顆 `回傳` 都沒有**。半條路沒回傳仍然只有執行期看得到。
- **作用域是扁平的**。hat 的 `yields` 與函式參數都算「已定義」，但不追可見範圍。
  §4.5 給的定位是「抓 90% 的錯字」——做窄比誤報好。
- **`data.set` 同名於函式參數時不警告 shadowing**（§5.4 要求）。它需要「這顆積木
  在哪個函式體裡」，而扁平作用域正好沒有那個資訊。
- **參數積木放錯地方只有執行期會說**。編輯期警告拿掉了（它在使用者拖到一半就叫）。
  真要早一點講，出口是 §4.6 的**存檔期**驗證，不是靜態檢查——`_validate_structure`
  要認得 `procedure.param` 的 `mutation.proc` 與祖先鏈（`回傳` 已經是這個形狀）。
- **一顆積木上兩格都打錯字時兩句話併成一則**，圖示指不出是哪一格。
- **`didYouMean` 只在候選裡找**，名字很多時可能建議不相干的；沒有測試釘住準確度。
- **檢查不進存檔路徑**。標著警告的專案存得下去、跑得動（§8.5「不阻擋連接」）。
- **孤兒警告與 `CheckRunner` 是兩本帳**（前者記在 Blockly warning、由下次互動清掉；
  後者每 200ms 重算）。「這顆積木為什麼有警告」有兩個地方要查。

### 2.2 `FieldText` 與欄位

- **前端有第二份解析器**（`ir/highlight.ts`）。它是顯示層、後端才是規格，兩邊由
  `field_messages.yaml` 與 AST 覆蓋測試釘住——但**釘的是訊息，不是語意**。
- **`ui.multiline` 存不下「強制單行」**。§4.2 只定義「要多行的名稱清單」，所以右鍵
  只有「多行 ↔ 自動」兩態；貼上長字串後想擠回一行沒有出口。
- **`rows` 只是捲動上限，不是高度**；空欄位上按「多行輸入」看不出變化（它改的是
  `maxLines`，而高度跟著值走）。
- **`Shift+Enter 換行` 沒有任何提示**（刻意關掉的），但「多行怎麼打」變成要自己發現。
- **autocomplete 只補 root**。`${a.` 之後不補——那要知道值長什麼樣子，是 §6.3 之後的事。
- **pill 沒有 hover 提示**。一格文字裡兩個 `${}` 只有一個錯時，積木上的警告圖示
  指不出是哪一個。要做得讓 `FieldText` 拿到「工作區有哪些名字」，那份資料現在只有
  `checks.ts` 有。
- **裝多行欄位的 reporter 仍然是膠囊**。zelos 的 reporter 一律 dynamic ROUNDED，要改
  得動父積木的 output shape，會連帶影響它插進哪種孔。
- **每次值變動都重跑分析並 `setWarningText`**（靜態檢查那半節流 200ms，這半是即時的）。
- **右鍵選單靠 `preconditionFn` 快取被點到的欄位**，依賴「precondition 一定先跑」這條
  Blockly 內部順序——**換版時要複驗**。
- **`bare` / `standalone` 是只有預覽積木在用的選項**，不進 manifest schema（刻意的），
  但它表示 `FieldText` 的選項有兩種來源。
- **「重新命名」的輸入框是 `window.prompt`**，樣式與其他地方對不上。

### 2.3 字面值與型別

- **字面值型別切換不進 undo 堆疊**（`createShadowBlock` 明寫 `recordUndo: false`）。
  `literals.test.ts` 有一條測試把這個行為釘住，**它變好的那天會紅**——屆時改成
  「一次 undo 退得回去」，而不是有人默默以為它一直都能 undo。
- **切完型別會失去 manifest 宣告的 `min` / `max`**。存檔重載會回來，它自己會修好。
- **下拉欄位的值沒有載入期驗證**。`fields.op` 寫成 `"wat"` 的 IR 存得進去。值域宣告在
  manifest 的 `options` 裡，`ir/schema.py` 有機會（也應該）比對它。對 `math_op` /
  `type.cast` / `debug.log` 的 `level` 全部成立。
- **回傳型別只影響形狀與靜態檢查，不影響執行**。宣告 `returns: number` 卻回傳文字時
  執行期不會抱怨。
- **參數積木沒有 `type` 的形狀提示**（`type: boolean` 也畫成橢圓）。刻意的（§8.5），
  但 `if <(旗標)>` 看不出「這一格期待布林」。

### 2.4 函式與重塑（`reshape.ts` / `params.ts`）

- **`reshapeProcedure` 沒有 catch**。第七輪那個 bug 的症狀之所以是「東西不見了」而不是
  「跳出錯誤」，就是例外漏到 `applyProcedure` 把後面的步驟一起帶走。成因修好了，
  **但那條路仍然沒有防護**——值得包一層 try/catch 變成一句看得見的話。
- **「改簽章」的三步順序只有單元測試守著，而那是一份複製品**。`params.test.ts` 的
  `apply()` 抄了 `App.tsx` 的三步，會漂移。**真正的修法是把那三步抽成共用函式。**
- **`fillDefinitionParams` 做三件事**（補孔、收拾被擠出來的、掛 drag strategy），名字只
  講第一件；它同時是「重塑後的收尾」而那件事由 `App.tsx` 記得呼叫——**併進
  `reshapeProcedure` 會少一個要記得的順序**。
- **函式體裡的參數改名靠整顆重建**，會換掉 Blockly 的積木物件（id 保住）。若之後有東西
  記著積木物件本身，重塑後那份記錄就是舊的（`RunDecorator` 每次 Run 重建，沒踩到）。
- **參數不能拖曳換順序**。只能刪掉再加，而新的排到最後面。
- **孤兒只錯開一點點**（+28 工作區單位，被 `bumpNeighbours` 再推成 +12/+4），看起來像疊
  在呼叫積木上。功能沒問題，只是不好認。
- **帽子上的參數被擠出來是靠 listener 收拾，不是靠擋住**。任何 reporter 都丟得進帽子的
  參數孔，`watchDefinitionParams` 會收掉它並補回孔——**但那一瞬間丟進去的積木還在孔裡，
  而它會被 `delete state.inputs` 在存檔時丟掉，目前沒有任何提示**。
- **`reads` 宣告目前沒有消費者**。`toolbox.ts::findVariableReader` 與後端的 `reads` 欄位、
  validator、測試都留著，它說的仍是真話，**但那是一段沒有人呼叫的程式**——下次動這塊時
  重新決定留或刪。
- **`buildToolbox` 的 `extras` 掛勾刪掉了**，沒有呼叫端。之後若又要「同一顆積木、不同預設
  欄位」的條目，要加回來——四行。
- **`procedure.param` 的題庫只有三題**。「參數 + `try_catch`」「參數在巢狀呼叫裡」還沒有。
- **改名時函式體裡的參數積木被重建這件事不在測試裡**（`reshape.test.ts` 驗的是呼叫積木
  與孤兒；參數那三條路只有程式碼與註解）。

### 2.5 對話框與工具箱

- **對話框沒有鍵盤路徑**：Esc 不關、Enter 不確定、開啟時沒有 focus trap。
- **預覽積木的右鍵選單靠 `.injectionDiv { overflow: visible }` 才顯示得完整**（容器只有
  9rem 高，六個項目會被裁成三個）。改的是 Blockly 自己的 class，**換版時要複驗**。
- **撐寬按鈕是動 Blockly 畫好的 DOM**（`FixedScaleFlyout.show()`），`FlyoutButton` 沒有
  宣告式寬度掛勾。**換版時要複驗。**
- **`select()` 搶不贏右鍵選單的焦點還原**，而且會留下清不掉的黃框（連叫三次三個黃框）。
  目前不叫它，只 `centerOnBlock`。之後要「從選單指出另一顆積木」得從 focus manager 下手。
- **拖曳過程中垃圾桶不會亮**（`wouldDelete_` 對 `deletable: false` 永遠是 false）。放開那
  一刻蓋子會動，但懸停時沒有預告。純視覺，要修比這件事本身貴。
- **定義帽子的右鍵多了一個「外部輸入」**（Blockly 內建）。按下去參數排成一直行，很怪但
  不會壞事。要收掉就註冊一個 `preconditionFn: 'hidden'` 蓋掉。
- **分類名維持「函式」**。改成「函式積木」會撐寬左邊分類欄（欄寬依最長分類名算，其他九個
  都是兩個字）；`name` 也出現在擴充功能面板與 `test_builtin_manifests.py` 的寫死名單裡。
- **頁面一載入就有 4 個工作區**（兩個 0 顆積木、DOM 已斷的 flyout，多半是 StrictMode）。
  數量不成長，不是洩漏，但它讓「工作區有幾個」不好用來判斷別的洩漏。
- **積木面板的縮放與畫布脫鉤之後，面板沒辦法放大了**。要獨立縮放得另給控制項。

### 2.6 執行期與後端

- **點一下就跑 = 每次都先存檔**（PUT + POST），沒有節流。單機沒痛感，接遠端後端要重看。
- **`block.enter` 沒帶展開後的字串**，所以 §8.5 的「滑過欄位看到實際送出的內容」還不存在。
  要動 `engine.py` 的兩處 emit。
- **`block.error` 沒有 traceback**（`BlockyError.to_dict()` 只有 type/code/message/blockId/
  hint），§8.3 的「點擊展開 traceback」目前只展得出 hint。
- **運算積木不發子步驟事件**（§4.7b），`block.exit` 只有最後那個值。
- **`var.set` 的窗口內收斂**（同名只留最後一次，§6.2）比設計文件原本寫的多。之後要做
  「變數變化的歷史曲線」時，這條收斂是第一個要重新談的。
- **§6.3 的 SQLite 落地沒做**：執行歷史只在記憶體、上限 50 個 Run、重啟就沒了；
  `GET /api/runs/{id}/events`（附錄 A）因此不存在；**`persist_*` 不跨後端重啟**（D12 明講
  要有的性質，目前是欠的）。
- **§5.5 的「清理有 5 秒上限」沒實作**，停止時只是 `cancel()`。
- **改了內建 manifest 要重啟後端**（`declarations` 在 import 期讀 YAML，vite HMR 幫不上），
  症狀是「積木長得跟改之前一樣」——很容易被當成前端沒吃到。
- **動態下拉（`source`）還是文字框**，要 `POST /api/extensions/{id}/dropdown/{source}`。
- **變數索引 `variables` 一律存空物件**（§4.5 說它是衍生索引）。監看面板吃執行中的
  `var.set` 事件、autocomplete 掃工作區，兩者都不吃它，所以繼續留空仍然誠實。
- **遞迴 headroom 是估的**：`PYTHON_FRAMES_PER_BLOCKY_FRAME = 24` 為經驗值，靠
  `RecursionError` 保險絲兜底。
- **`blocky serve` 沒有正式打包測試**：`[project.scripts]` 加了，只驗過
  `python -m blocky.cli`，沒驗過 `pip install` 之後的 `blocky` 指令。
- **變數面板的開關存 localStorage**，§16 Q15（偏好放哪）未定案。

### 2.7 測試缺口

- **半形單字置中沒有單元測試**：`FieldText.test.ts` 是 jsdom，量不到
  `getComputedTextLength` / `getBBox`，而斷言正是「文字中心 == 積木中心」。目前只有瀏覽器
  實測守著（`i` / `1` / `l` / `你` / `ab` 偏移皆為 0）。
- **值氣泡的 hover 凍結沒有測試**（rAF + 版面），是 `run/store` 之外唯一沒測試的執行期 UI。
- **`data.list_insert` 用 `len+1` 正規化索引，無測試**，可疑。
- **`真` / `假` 沒進 §17.2 以外的題目**（題庫一題）。「布林字面值 + `type.of`」「存檔來回」
  這些邊界靠前端單元測試守。
- **§17.2 有幾列還寫不出題目**：`concurrency` 的 drop/queue/restart、`CancelledError` 穿透、
  §6.3 的 SQLite 落地——都要等 P2 的機制存在。
- **那 45 顆沒有題目的積木，參數名有 AST 測試守著**，缺的是「它真的跑得動」那半。

### 2.8 設計文件未決題

- **§16 Q10（目標使用者：教育 vs 開發者）最遲要在 P1 開始前決定**，它決定手寫哪三個積木
  包。P0b 的範圍已經沒有東西擋著它。
- **Q1、Q3–Q9、Q11、Q12、Q14、Q15、Q17 仍未決**（Q16 在第 6 步決了、Q18 在第五輪決了）。
- **P1 剩下的部分刻意延後**：SubprocessHost 與跨 process 反向通道（§7.6）、`ctx.http`
  （§7.4）、secret 值遮蔽（§12.2）、migrations（§13.2）。

## 3. 下一次的第一個 TODO

1. **等第九輪實測回饋。** 第 5c～7 步改的東西幾乎全部是使用者按了會直接碰到的。
2. **函式分類的介面整個重看**（回饋明講「之後再改」）。連帶要決定三件，都在第 2 節：
   分類名要不要改成「函式積木」（§2.5）、`reads` 宣告留不留（§2.4）、參數要不要有型別的
   形狀提示（§2.3）。
3. **兩個「該重構」已經記在第 2 節**，下次動那一塊時一起做：把 `applyProcedure` 的三步抽成
   共用函式、把 `fillDefinitionParams` 併進 `reshapeProcedure`（§2.4）。

然後才是 P0c / P1 的分岔，而那個分岔要 §16 Q10 先決定。

**時程**：P0b 仍在 **5～7 週**這條線上。

---

### 三件從八輪回饋裡學到、值得帶到下一輪的事

1. **「規則對了，但少走了一條路。」** 每一輪幾乎都是這個形狀——`取得 (參數名)` 讀得到值卻
   說錯話；「入口只有一個」守住了規則卻讓最自然的動作沒有反應；分類裡多列一份看起來體貼
   卻少了上下文。共同點是每次都省了一步「使用者真的會怎麼做」，**而那一步只有把東西交出去
   才問得到**。
2. **單元測試每一步都綠，串起來才炸。** `reshapeProcedure` 有 9 題、`fillDefinitionParams`
   有 6 題，bug 出在「`App.tsx` 把它們接起來」的那條縫上。補的 `apply()` helper 是一份
   **複製**的順序，真正的修法是讓那三步變成一個函式（記在 §2.4）。
3. **規則對、時機錯。** 「參數只能放在定義它的函式裡」是真話，但那條檢查在使用者還沒把積木
   放到定下來的位置時就開口。加檢查時要問的不只是「它說的對不對」，還有**「它會在哪一刻
   說」**——每次拖曳都閃一下的警告，就是 §4.5 那句「一個會叫的假警報最後會被忽略」講的東西。
