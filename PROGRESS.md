# PROGRESS

> 這份文件只做一件事：**交接**。它記「現在在哪裡、什麼還沒解決、下一步做什麼」。
> 規格與決議在 [`docs/design.md`](docs/design.md)（v0.16），實作經過在 `git log`。
> 兩邊已經有的東西，這裡不重複。

最後更新：2026-08-30

## 1. 現況

**P0b 第 1～8 步全部做完**，含九輪瀏覽器實測回饋。**下一個分岔是 P0c / P1，而它卡
§16 Q10**（目標使用者：教育 vs 開發者）——那是現在唯一擋路的未決題。編輯器現在有：
`FieldText`（`${}` pill、autocomplete、多行、右鍵選單）、字面值型別切換、運算積木、
`≈` / `≉`、執行前的三條靜態檢查、`建立一個積木` 對話框（分段浮動工具列 + 鍵盤路徑）
與 D26 簽章模板、`procedure.param` 參數積木、形狀重塑與孤兒處理、cap block
（`回傳` / `control.stop`）。

```
cd backend && .venv/bin/python -m pytest      # 648 passed, 5 skipped（題庫 84 題）
cd packages/editor && npm test                # 336 passed（13 檔）
cd packages/editor && npx tsc --noEmit        # 乾淨
```

題庫覆蓋 43/88 顆積木（49%）。

**第 8 步做了四件**（§15、design.md v0.16）：分段的浮動工具列（`‹` `🗑` `›`）、對話框
的鍵盤路徑（Esc / Enter / focus trap）、錯誤紅框與紅氣泡改成淡出、只有落單的 reporter
才冒值氣泡。後兩件是**改決定**，design.md §8.3 的那幾句已經跟著改掉。四件都在瀏覽器裡
實測過（單元測試碰不到的那幾條，見 §2.5、§2.7），而實測抓到一個真的 bug——Esc 會連
整個對話框一起關掉，見 §2.5 的 capture 那條。

## 2. 未解決問題與已知限制

### 2.1 靜態檢查（`ir/checks.ts`）

- **「有路徑未回傳」是退化版**。§4.6 要的是真的算每一條路徑，現在守的是最有把握
  的那一格：**一顆 `回傳` 都沒有**。半條路沒回傳仍然只有執行期看得到。
- **作用域是扁平的**。hat 的 `yields` 與函式參數都算「已定義」，但不追可見範圍。
  §4.5 給的定位是「抓 90% 的錯字」——做窄比誤報好。
- **`data.set` 同名於函式參數時不警告 shadowing**（§5.4 要求）。它需要「這顆積木
  在哪個函式體裡」，而扁平作用域正好沒有那個資訊。
- **參數積木放錯地方只有執行期會說**。編輯期警告拿掉了（它在使用者拖到一半就叫）。
  真要早一點講，出口是 §4.6 的**存檔期**驗證，不是靜態檢查——`_validate_structure`
  要認得 `procedure.param` 的 `mutation.proc` 與祖先鏈（`回傳` 已經是這個形狀）。
- **一顆積木上兩格都打錯字時兩句話併成一則**，圖示指不出是哪一格。
- **`didYouMean` 只在候選裡找**，名字很多時可能建議不相干的；沒有測試釘住準確度。
- **檢查不進存檔路徑**。標著警告的專案存得下去、跑得動（§8.5「不阻擋連接」）。
- **孤兒警告與 `CheckRunner` 是兩本帳**（前者記在 Blockly warning、由下次互動清掉；
  後者每 200ms 重算）。「這顆積木為什麼有警告」有兩個地方要查。

### 2.2 `FieldText` 與欄位

- **前端有第二份解析器**（`ir/highlight.ts`）。它是顯示層、後端才是規格，兩邊由
  `field_messages.yaml` 與 AST 覆蓋測試釘住——但**釘的是訊息，不是語意**。
- **`ui.multiline` 存不下「強制單行」**。§4.2 只定義「要多行的名稱清單」，所以右鍵
  只有「多行 ↔ 自動」兩態；貼上長字串後想擠回一行沒有出口。
- **`rows` 只是捲動上限，不是高度**；空欄位上按「多行輸入」看不出變化（它改的是
  `maxLines`，而高度跟著值走）。
- **`Shift+Enter 換行` 沒有任何提示**（刻意關掉的），但「多行怎麼打」變成要自己發現。
- **autocomplete 只補 root**。`${a.` 之後不補——那要知道值長什麼樣子，是 §6.3 之後的事。
- **pill 沒有 hover 提示**。一格文字裡兩個 `${}` 只有一個錯時，積木上的警告圖示
  指不出是哪一個。要做得讓 `FieldText` 拿到「工作區有哪些名字」，那份資料現在只有
  `checks.ts` 有。
- **裝多行欄位的 reporter 仍然是膠囊**。zelos 的 reporter 一律 dynamic ROUNDED，要改
  得動父積木的 output shape，會連帶影響它插進哪種孔。
- **每次值變動都重跑分析並 `setWarningText`**（靜態檢查那半節流 200ms，這半是即時的）。
- **右鍵選單靠 `preconditionFn` 快取被點到的欄位**，依賴「precondition 一定先跑」這條
  Blockly 內部順序——**換版時要複驗**。
- **`bare` / `standalone` 是只有預覽積木在用的選項**，不進 manifest schema（刻意的），
  但它表示 `FieldText` 的選項有兩種來源。
- **「重新命名」的輸入框是 `window.prompt`**，樣式與其他地方對不上。

### 2.3 字面值與型別

- **字面值型別切換不進 undo 堆疊**（`createShadowBlock` 明寫 `recordUndo: false`）。
  `literals.test.ts` 有一條測試把這個行為釘住，**它變好的那天會紅**——屆時改成
  「一次 undo 退得回去」，而不是有人默默以為它一直都能 undo。
- **切完型別會失去 manifest 宣告的 `min` / `max`**。存檔重載會回來，它自己會修好。
- **孔裡插著 reporter 時，底下那顆影子的值存不下來**。IR 的 `kind: block` 只說「這個孔由
  一顆積木求值」，說不出被蓋住的影子是什麼（§4.2 的四種 `kind` 裡沒有它）。載入時補的是
  **manifest 宣告的預設影子**（`deserialize.ts::shadowUnder`，跟工具箱同一份資料），所以
  「拔掉 reporter 之後那格還編輯得了」成立，但使用者插 reporter 之前打過的字回不來。
  要救得動 IR schema，而那等於為了純呈現狀態加一個 `kind`。同一個縫也讓 `ui.multiline`
  在被蓋住的影子上存不下來（`serialize.ts::readUi` 讀到的是那顆 reporter）。
- **下拉欄位的值沒有載入期驗證**。`fields.op` 寫成 `"wat"` 的 IR 存得進去。值域宣告在
  manifest 的 `options` 裡，`ir/schema.py` 有機會（也應該）比對它。對 `math_op` /
  `type.cast` / `debug.log` 的 `level` 全部成立。
- **回傳型別只影響形狀與靜態檢查，不影響執行**。宣告 `returns: number` 卻回傳文字時
  執行期不會抱怨。
- **參數積木沒有 `type` 的形狀提示**（`type: boolean` 也畫成橢圓）。刻意的（§8.5），
  但 `if <(旗標)>` 看不出「這一格期待布林」。

### 2.4 函式與重塑（`reshape.ts` / `params.ts`）

- **影子有三條路長出來，而重塑那條曾經漏掉**（第十輪回饋修掉的 bug）：Blockly 的 JSON
  積木定義**沒有地方宣告影子**，所以影子只能從工具箱條目（`toolbox.ts::toToolboxBlock`）
  或序列化狀態（`deserialize.ts::shadowUnder`、`reshape.ts::fillNewInputs`）來。加一個
  參數之後，畫布上那顆呼叫積木的新孔是「把舊 state 照新定義 append 一次」生出來的，
  舊 state 裡沒有它 → 空孔，症狀是**一格點不進去、打不了字的深色膠囊**。三條路現在都
  讀 `registered.shadows`（manifest 算出來的同一份），但**它們仍然是三段各自的程式**——
  下次再加一條產生積木的路徑，記得問「影子誰補」。

- **`reshapeProcedure` 沒有 catch**。第七輪那個 bug 的症狀之所以是「東西不見了」而不是
  「跳出錯誤」，就是例外漏到 `applyProcedure` 把後面的步驟一起帶走。成因修好了，
  **但那條路仍然沒有防護**——值得包一層 try/catch 變成一句看得見的話。
- **「改簽章」的三步順序只有單元測試守著，而那是一份複製品**。`params.test.ts` 的
  `apply()` 抄了 `App.tsx` 的三步，會漂移。**真正的修法是把那三步抽成共用函式。**
- **`fillDefinitionParams` 做三件事**（補孔、收拾被擠出來的、掛 drag strategy），名字只
  講第一件；它同時是「重塑後的收尾」而那件事由 `App.tsx` 記得呼叫——**併進
  `reshapeProcedure` 會少一個要記得的順序**。
- **函式體裡的參數改名靠整顆重建**，會換掉 Blockly 的積木物件（id 保住）。若之後有東西
  記著積木物件本身，重塑後那份記錄就是舊的（`RunDecorator` 每次 Run 重建，沒踩到）。
- **參數不能拖曳換順序**。只能刪掉再加，而新的排到最後面。**已經有規格了**
  （§8.5 的分段浮動工具列，v0.15），還沒做。資料層是免費的——`Draft.segments`
  就是版面，移動一段等於陣列搬一格；成本全在 UI。
- **孤兒只錯開一點點**（+28 工作區單位，被 `bumpNeighbours` 再推成 +12/+4），看起來像疊
  在呼叫積木上。功能沒問題，只是不好認。
- **帽子上的參數被擠出來是靠 listener 收拾，不是靠擋住**。任何 reporter 都丟得進帽子的
  參數孔，`watchDefinitionParams` 會收掉它並補回孔——**但那一瞬間丟進去的積木還在孔裡，
  而它會被 `delete state.inputs` 在存檔時丟掉，目前沒有任何提示**。
- **`reads` 宣告目前沒有消費者**。`toolbox.ts::findVariableReader` 與後端的 `reads` 欄位、
  validator、測試都留著，它說的仍是真話，**但那是一段沒有人呼叫的程式**——下次動這塊時
  重新決定留或刪。
- **`buildToolbox` 的 `extras` 掛勾刪掉了**，沒有呼叫端。之後若又要「同一顆積木、不同預設
  欄位」的條目，要加回來——四行。
- **`procedure.param` 的題庫只有三題**。「參數 + `try_catch`」「參數在巢狀呼叫裡」還沒有。
- **改名時函式體裡的參數積木被重建這件事不在測試裡**（`reshape.test.ts` 驗的是呼叫積木
  與孤兒；參數那三條路只有程式碼與註解）。

### 2.5 對話框與工具箱

- **浮動工具列是滑鼠專用的**（`tabIndex={-1}`）。鍵盤到不了 `‹` `🗑` `›`，所以「用鍵盤
  換分段順序」沒有出口。它們只在點進一格時存在，而那一刻焦點屬於那一格的欄位——
  放進 Tab 圈等於讓 focus trap 繞經一排隨時會消失的按鈕。
- **鍵盤也走不進預覽積木**。`focusableIn` 用「不是 HTMLElement」濾掉 Blockly 的 SVG
  節點，因為把那個帶 tabindex 的 `<g>` 放進循環會讓整個 trap 壞掉（實測：焦點在它身上
  「消失」，下一次 Tab 永遠回到第一個，`×` 與工作區之間來回彈）。代價是**只有滑鼠
  改得了分段的文字**。
- **Esc 關掉欄位編輯器之後，浮動工具列還留著**。模型是「使用者點進了哪一格」而不是
  「哪一格的編輯器開著」（§8.5 的說法是後者）——功能上沒差，但兩者不是同一件事。
- **工具列的判斷依賴 Blockly 的兩條內部順序**，換版時要複驗：按鈕靠 `pointerdown` +
  `preventDefault()` 讓焦點不動（`WidgetDiv` 是**靠焦點活著**的，實測它只掛 focusin /
  focusout），而 keydown 必須 **capture** 才問得到「現在編輯器開不開著」（實測：Blockly
  先收掉 `WidgetDiv`，冒泡回來時已經關了——Esc 於是會關掉整個對話框，而使用者剛打的
  字全沒了。這個 bug 真的發生過，capture 是修法）。
- **工具列的位置是量出來的**（`segmentRect` → `getBoundingClientRect`），jsdom 測不到，
  只有瀏覽器實測守著——與半形單字置中、flyout 版面同一類（§2.7）。純函數那一半
  （`moveSegment` / `segmentActions`）有測試。
- **用工具列刪掉一格之後工具列收起來**（不遞補給下一段）。刻意的：遞補等於讓「再按
  一次」刪掉一段沒被指名的東西。代價是連刪兩格要重點一次。
- **預覽積木的右鍵選單靠 `.injectionDiv { overflow: visible }` 才顯示得完整**（容器只有
  9rem 高，六個項目會被裁成三個）。改的是 Blockly 自己的 class，**換版時要複驗**。
  浮動工具列是這個 hack 的**第二個消費者**（它畫在預覽區的上緣之外）。
- **積木面板是固定寬度（`FLYOUT_WIDTH = 300`），超出的積木直接被裁掉**。Blockly 原本
  「量最寬的那顆積木」的作法讓**一顆積木就能決定整個面板**（積木包作者寫一顆很長的
  積木，面板就吃掉半個畫布）。裁掉的代價是那一顆在面板裡看不完整——拖出來就完整了。
  刻意**不加橫向捲軸**：那是在為「面板應該要能看完整」這個不成立的前提付錢。裁切是
  免費的（flyout 是 `<svg>`，SVG 根元素本來就裁自己的內容）。
- **面板寬度可以拖曳**（右緣的 `col-resize` 把手，`FlyoutResizer`），拉到 0 就整條收起來，
  **出口是點左邊的分類欄**（那時畫面上唯一還看得見、又與「我想要積木」有關的東西）。
  寬度存 localStorage（`prefs.ts`，同變數面板那條，§16 Q15 未定案）。三件已知的邊：
  把手的位置是**量**出來的（分類欄寬度依最長分類名而定，所以用 `ResizeObserver` 而不是
  寫死 60——第一版寫死錯的類別名 `.blocklyToolboxDiv`，症狀就是把手與邊界差了一欄）；
  拖曳中不寫偏好（放開才寫）；**沒有單元測試**（版面，同 §2.7 那一族）。
- **工具箱按鈕跟著面板寬度走，兩個方向**：以前有一條「按鈕比目標寬就不動它」的守衛，
  那時目標是「最寬的那顆積木」（比按鈕文字還窄是常有的事）；現在目標是使用者拉出來的
  寬度，留著那條守衛的症狀是「拉寬再拉窄，按鈕卡在最寬的那一次」。窄到文字放不下時
  **改成靠左並裁掉尾巴**（`placeButtonText`）——置中的文字會從白色圓角矩形的兩邊溢出去，
  看起來不是「被裁掉」是「畫壞了」。裁切要自己掛 `clipPath`：`<text>` 不會被同一群組裡的
  `<rect>` 裁到，而 flyout 那層的裁切在按鈕右邊還有 18px 留白。
- **撐寬按鈕是動 Blockly 畫好的 DOM**（`FixedScaleFlyout.stretchButtons()`），`FlyoutButton`
  沒有宣告式寬度掛勾。目標寬度是 `reflowInternal_` 的**反函數**（「最寬的那顆積木有多寬」），
  不是 flyout 的寬度——因為 `element.width` 正是 reflow 算 flyout 寬度時的輸入之一，照 flyout
  撐開等於自己餵自己：不經過 `show()` 的 reflow（視窗縮放、`resizeContents`）會把 flyout 撐寬
  12px 而按鈕沒跟上。**那條反函數抄的是 Blockly 的式子，換版時要重新對一次。**
  （寬度改成固定之後，那條「自己餵自己」的迴圈已經不可能發生——`reflowInternal_`
  不再讀任何內容的寬度。式子還在，因為按鈕仍然要跟積木左右切齊。）
- **flyout 的版面沒有測試**（jsdom 量不到 `getBoundingClientRect`），只有瀏覽器實測守著：
  按鈕與最寬的積木左右留白都是 12 / 18px，且 `dispatchEvent(new Event('resize'))` 前後
  flyout 寬度不變（299 → 299）。
- **`select()` 搶不贏右鍵選單的焦點還原**，而且會留下清不掉的黃框（連叫三次三個黃框）。
  目前不叫它，只 `centerOnBlock`。之後要「從選單指出另一顆積木」得從 focus manager 下手。
- **拖曳過程中垃圾桶不會亮**（`wouldDelete_` 對 `deletable: false` 永遠是 false）。放開那
  一刻蓋子會動，但懸停時沒有預告。純視覺，要修比這件事本身貴。
- **定義帽子的右鍵多了一個「外部輸入」**（Blockly 內建）。按下去參數排成一直行，很怪但
  不會壞事。要收掉就註冊一個 `preconditionFn: 'hidden'` 蓋掉。
- **分類名維持「函式」**。改成「函式積木」會撐寬左邊分類欄（欄寬依最長分類名算，其他九個
  都是兩個字）；`name` 也出現在擴充功能面板與 `test_builtin_manifests.py` 的寫死名單裡。
- **頁面一載入就有 4 個工作區**（兩個 0 顆積木、DOM 已斷的 flyout，多半是 StrictMode）。
  數量不成長，不是洩漏，但它讓「工作區有幾個」不好用來判斷別的洩漏。
- **積木面板的縮放與畫布脫鉤之後，面板沒辦法放大了**。要獨立縮放得另給控制項。
- **段落標題靠 class 字串被排除在分類邊界之外**（§8.1 的 `section`）。
  `theme.ts::toolboxItemIsLabel` 認的是 `blocky-section-label` 這個 `web-class`，
  它由 `toolbox.ts` 掛上去——**兩個檔案共用一個字串常數，而它沒有被抽出來**。
  排除失敗的症狀是「點分類捲錯地方」，不是壞掉，所以不會馬上被發現。
- **三個間隔數字（12 / 28 / 8）沒有瀏覽器實測**。單元測試釘的是「展開成哪幾個
  條目」，不是「看起來像不像一段」。

### 2.6 執行期與後端

- **點一下就跑 = 每次都先存檔**（PUT + POST），沒有節流。單機沒痛感，接遠端後端要重看。
- **`block.enter` 沒帶展開後的字串**，所以 §8.5 的「滑過欄位看到實際送出的內容」還不存在。
  要動 `engine.py` 的兩處 emit。
- **錯誤的紅框淡出之後，`error` 這個 phase 還在**（class 也還掛著，只是動畫把它畫成
  透明）。所以「同一顆積木在**同一次執行裡**錯第二次」不會重新倒數——`RunDecorator`
  看到 class 沒變就跳過，而 CSS 動畫要 class 真的被拆掉才會重跑。跨執行沒問題
  （`begin()` 清空 blocks → class 拆掉 → 重新掛上；實測 stroke-opacity 走 0 → 1 → 0）。
  目前沒有 `try_catch`，一顆積木在一次執行裡錯兩次還做不出來，所以這條先記著。
- **滑鼠停在氣泡上凍得住氣泡、凍不住框**（框是 CSS 動畫、氣泡是 rAF 迴圈）。這是
  §8.3 明寫的取捨：框是提示、氣泡才是內容。
- **`speaks()` 讀的是「當下」的連接狀態**（`outputConnection.isConnected()`），而它只在
  `blocks` 這個 Map 換掉時重算。執行中把一顆本來插著的 reporter 拔出來，它不會因此
  補冒一個氣泡——下一個事件窗口才算得到。
- **`block.error` 沒有 traceback**（`BlockyError.to_dict()` 只有 type/code/message/blockId/
  hint），§8.3 的「點擊展開 traceback」目前只展得出 hint。
- **運算積木不發子步驟事件**（§4.7b），`block.exit` 只有最後那個值。
- **`var.set` 的窗口內收斂**（同名只留最後一次，§6.2）比設計文件原本寫的多。之後要做
  「變數變化的歷史曲線」時，這條收斂是第一個要重新談的。
- **§6.3 的 SQLite 落地沒做**：執行歷史只在記憶體、上限 50 個 Run、重啟就沒了；
  `GET /api/runs/{id}/events`（附錄 A）因此不存在；**`persist_*` 不跨後端重啟**（D12 明講
  要有的性質，目前是欠的）。
- **§5.5 的「清理有 5 秒上限」沒實作**，停止時只是 `cancel()`。
- **欄位編輯器的樣式有兩個來源，而 Blockly 那一份是 inline style**。實測：它把
  `color` 與 `border-radius`（只有這兩個）寫在 input 的 `style` 上，所以說明文字那一格
  的白字與方角要 `!important` 才蓋得過；`background` 與 `caret-color` 它沒寫，照一般權重
  就過。**換版時要複驗**——哪天它多寫一個屬性，症狀會是「那一格編輯中長得跟旁邊的
  名稱格一樣」，而那正是 `bare` 要避免的事。
- **改了 `FieldText`（或任何註冊給 Blockly 的類別）要重新整理頁面**。vite 會把改動一路
  propagate 到 `App.tsx` 而 React Fast Refresh 收下它，但 Blockly 的 field 註冊表裡放的
  仍然是**舊的那個類別**——症狀是「CSS 生效了、行為沒變」，很容易被當成改錯地方。與
  下一條（改 manifest 要重啟後端）是同一個家族。
- **改了內建 manifest 要重啟後端**（`declarations` 在 import 期讀 YAML，vite HMR 幫不上），
  症狀是「積木長得跟改之前一樣」——很容易被當成前端沒吃到。
- **改了 IR 的 pydantic 模型也要重啟後端**，而且症狀更兇：`Strict` 是 `extra="forbid"`，
  所以前端一送出新欄位就是 **422「Extra inputs are not permitted」**，存檔整個失敗。
  加欄位的完整順序是 `schema.py` → `tools/export_schema.py` → `npm run gen:types`
  → **重啟後端**。
- **動態下拉（`source`）還是文字框**，要 `POST /api/extensions/{id}/dropdown/{source}`。
- **變數索引 `variables` 一律存空物件**（§4.5 說它是衍生索引）。監看面板吃執行中的
  `var.set` 事件、autocomplete 掃工作區，兩者都不吃它，所以繼續留空仍然誠實。
- **遞迴 headroom 是估的**：`PYTHON_FRAMES_PER_BLOCKY_FRAME = 24` 為經驗值，靠
  `RecursionError` 保險絲兜底。
- **`blocky serve` 沒有正式打包測試**：`[project.scripts]` 加了，只驗過
  `python -m blocky.cli`，沒驗過 `pip install` 之後的 `blocky` 指令。
- **變數面板的開關存 localStorage**，§16 Q15（偏好放哪）未定案。

### 2.7 測試缺口

- **半形單字置中沒有單元測試**：`FieldText.test.ts` 是 jsdom，量不到
  `getComputedTextLength` / `getBBox`，而斷言正是「文字中心 == 積木中心」。目前只有瀏覽器
  實測守著（`i` / `1` / `l` / `你` / `ab` 偏移皆為 0）。
- **值氣泡的 hover 凍結沒有測試**（rAF + 版面），是 `run/store` 之外唯一沒測試的執行期 UI。
- **淡出的時間沒有單元測試**：錯誤紅框那半是 CSS 動畫（`index.css` 的兩段 keyframes），
  氣泡那半在 rAF 迴圈裡。兩者都只有瀏覽器實測守著（實測到的是：class 掛上後 stroke-opacity
  維持 1 約 2 秒，接著 0.4 秒內落到 0；重跑會 0 → 1 → 0 重新來一次）。
- **浮動工具列的版面沒有測試**（同上一條家族）：畫得出哪幾個圖示（`segmentActions`）與
  搬一格之後的簽章（`moveSegment`）有測試，「它有沒有貼在那一格正上方」沒有。
- **focus trap 只有算術那一半有測試**（`nextFocusIndex`、`modalKeyAction`）。「Tab 真的
  停在哪一顆按鈕上」是 DOM 的事，vitest 跑在 node 環境（沒有 jsdom），所以那一半也是
  瀏覽器實測（實測：7 個控制項兩個方向都繞得回來）。
- **`data.list_insert` 用 `len+1` 正規化索引，無測試**，可疑。
- **`真` / `假` 沒進 §17.2 以外的題目**（題庫一題）。「布林字面值 + `type.of`」「存檔來回」
  這些邊界靠前端單元測試守。
- **§17.2 有幾列還寫不出題目**：`concurrency` 的 drop/queue/restart、`CancelledError` 穿透、
  §6.3 的 SQLite 落地——都要等 P2 的機制存在。
- **那 45 顆沒有題目的積木，參數名有 AST 測試守著**，缺的是「它真的跑得動」那半。

### 2.8 設計文件未決題

- **§16 Q10（目標使用者：教育 vs 開發者）最遲要在 P1 開始前決定**，它決定手寫哪三個積木
  包。P0b 的範圍已經沒有東西擋著它。
- **Q1、Q3–Q9、Q11、Q12、Q14、Q15、Q17、Q19 仍未決**（Q10 在 P0b 結束時決了：**開發者路線**；
  Q16 在第 6 步決了、Q18 在第五輪決了）。
- **Q19 是新的**（`如果⋯否則如果⋯` 的 `+` `−`）：它真正要的是 **manifest 的「可重複參數群組」**，
  而不是 if 的一個功能——`try_catch` 的多個 catch、HTTP 的多個 header 是同一個形狀。排在 P2，
  要先讓 P1 的三個手寫包磨過一輪。現況的替代是把 `如果` 拖進 `否則` 裡（Scratch 的作法），
  代價是階梯狀縮排。
- **P1 剩下的部分刻意延後**：SubprocessHost 與跨 process 反向通道（§7.6）、`ctx.http`
  （§7.4）、secret 值遮蔽（§12.2）、migrations（§13.2）。

## 3. 下一次的第一個 TODO

1. ~~決定 §16 Q10~~ **已決議：開發者路線**（design.md v0.17）。連帶：**「P0c」不存在**，
   P0b 之後直接進 P1；三個手寫包（`http` / `openai` / `discord`）的選擇與順序不變，因為
   它們本來就是開發者取向。**所以下一步是 P1**，但 P0 還沒結案——見下面兩件。
2. **驗收 1 走到一半就找到一件事：§15 的驗收腳本照字面跑不起來。**
   「重複 10 次 → `改變 count 增加 1` → log」——`改變` 讀了一個從沒 `設定` 過的變數，而
   §4.5 說那是**錯誤**。執行期給的訊息是「未知變數 "count"，「改變」不能用在還沒建立的
   變數上」，而靜態檢查在按執行之前就已經標了黃色警告，所以整條路**在編輯期就看得見**
   ——語意與提示都是對的，**該改的是驗收的文字**（前面補一顆 `設定 count 為 0`），不是
   放寬 §4.5。順帶驗到的：`取得` 的 autocomplete 打 `cou` 就跳出 `count`，全程沒有任何
   「建立變數」按鈕——那正是驗收 1 真正要問的事。**四條驗收還沒走完**（1 走到執行、
   2～4 未走）。
3. **等第九輪之後的實測回饋。** 第 8 步改的四件全部是使用者按了會直接碰到的，而前八輪
   的紀錄是**每一輪都會問出一件寫不進規格的事**。特別想聽的三件：
   - 浮動工具列的三個圖示夠不夠自明（`‹` `›` 沒有文字）；
   - 錯誤淡出之後，「錯誤去哪了」找不找得到（log 與 topbar 是唯一的兩處）；
   - 落單才冒泡之後，除錯時會不會反而想看插在孔裡那顆的值（§2.6 記著這個取捨）。
4. **函式分類的介面整個重看**（回饋明講「之後再改」）。連帶要決定三件，都在第 2 節：
   分類名要不要改成「函式積木」（§2.5）、`reads` 宣告留不留（§2.4）、參數要不要有型別的
   形狀提示（§2.3）。
5. **兩個「該重構」已經記在第 2 節**，下次動那一塊時一起做：把 `applyProcedure` 的三步抽成
   共用函式、把 `fillDefinitionParams` 併進 `reshapeProcedure`（§2.4）。
6. **鍵盤路徑只做了一半**（§2.5）：對話框的 Tab / Esc / Enter 有了，但**鍵盤走不進預覽
   積木**，於是分段的文字與順序都只有滑鼠改得動。要補得先解決「Blockly 的 SVG 節點放進
   focus 循環會讓焦點消失」那件事，那是 Blockly focus manager 的事，不是這個對話框的。

**時程**：P0b 的八步做完了，原估 4～6 週 + 第 5b 步插隊的一週。下一段的長度要等 Q10。

---

### 四件從實測裡學到、值得帶到下一輪的事

1. **「規則對了，但少走了一條路。」** 每一輪幾乎都是這個形狀——`取得 (參數名)` 讀得到值卻
   說錯話；「入口只有一個」守住了規則卻讓最自然的動作沒有反應；分類裡多列一份看起來體貼
   卻少了上下文。共同點是每次都省了一步「使用者真的會怎麼做」，**而那一步只有把東西交出去
   才問得到**。
2. **單元測試每一步都綠，串起來才炸。** `reshapeProcedure` 有 9 題、`fillDefinitionParams`
   有 6 題，bug 出在「`App.tsx` 把它們接起來」的那條縫上。補的 `apply()` helper 是一份
   **複製**的順序，真正的修法是讓那三步變成一個函式（記在 §2.4）。
3. **規則對、時機錯。** 「參數只能放在定義它的函式裡」是真話，但那條檢查在使用者還沒把積木
   放到定下來的位置時就開口。加檢查時要問的不只是「它說的對不對」，還有**「它會在哪一刻
   說」**——每次拖曳都閃一下的警告，就是 §4.5 那句「一個會叫的假警報最後會被忽略」講的東西。
4. **「該不該接這個鍵」問對了，「什麼時候問」問錯了。** 第 8 步的 `modalKeyAction` 有 9 題
   單元測試、每一條規則都對，而 Esc 仍然會關掉整個對話框——因為它問「欄位編輯器開著嗎」
   的那一刻，Blockly 已經把編輯器關掉了。**測得到的是規則，測不到的是順序**：凡是「我讀的
   狀態別人也在改」的判斷，都要問一次「我跑在誰前面」。這與第 3 條（規則對、時機錯）是同一
   家族的東西，差別只在那次錯在使用者的時間軸上，這次錯在事件的順序上。
