# PROGRESS

最後更新：2026-08-28（第 5b 步：第一次實測回饋全部收掉）｜ 11 commits
｜ 後端 `cd backend && .venv/bin/python -m pytest` → 480 passed, 3 skipped（題庫 68 題）
｜ 前端 `cd packages/editor && npm test` → 91 passed（23 define + 57 round-trip + 11 run store）

## 1. 本次完成

**P0b 第 5b 步：編輯器體感**——十件事全部做完，都來自把編輯器交到使用者手上
跑一次之後的回饋。

**擋路的那條沒了**：畫布上留一顆沒接 hat 的積木現在存得了檔，而且**點它就能
跑**。這兩件事合起來才是 Scratch 的工作方式——先拉幾顆試試看，點一下問它會回
什麼，然後才決定要不要接到腳本上。

### 後端

- **刪掉一條檢查**（`ir/schema.py`）。「腳本最上面必須是事件積木」擋的是使用者
  天天在做的事；而它想擋的「hat 出現在堆疊中間」由 `_require_shape`（`next` 接
  的積木必須是 command）擋，訊息還更準確。**是刪掉不是拆成兩條**，兩個測試各自
  守著這兩半（`test_api.py`）。引擎一行都沒改——§5.1 的觸發條件本來就是「top 的
  opcode 等於這次的 trigger」，落單堆疊自己就選不中。
- **`Entry`**（`interpreter/engine.py`）：一條 thread 從哪裡開始。綠旗由 trigger
  選腳本、起點永遠是 `hat.next`；「點一下就跑」的起點是那顆積木自己，而它**可能
  是 reporter**——那時候要求值不是執行。兩種起點的差別全部收在這個型別裡，
  `_run_thread` 之後的路一條都不分岔（scope、事件、錯誤、取消都同一份）。
- `entry_for(blockId)` 定了兩條 Scratch 規則：點 reporter 只求值那一顆；點其餘
  的從**堆疊頂端**起跑。第二條不是省事的選擇——從中間插進去會讓 `repeat` 的迴圈
  體脫離它的迴圈，那是畫面上看不出來的執行。
- 起點在**建立 Run 之前**解析（`runs/manager.py`），所以「畫布上有、存檔裡沒有」
  是一個 422，而不是一個開始了又立刻死掉、還占著一格執行歷史的 Run。
- 題庫 +5 題（`control/click_*`、`lone_stack_*`）。`Case` 多了 `start`，
  `expect` 多了 `value`——起點是 reporter 的題目，答案只在事件裡，沒有這一欄
  就只剩黃金軌跡在守，等於沒有手寫規格。
- **內建宣告 ↔ handler 的第三個一致性測試**：直接讀 `builtins/*.py` 的 AST，
  比對 `t.value(b, "…")` 讀的 key 與 manifest 的 `args`，連 field／輸入孔的分類
  一起比。它**不需要題目**，87 顆全部守得到——補的正是「題庫只覆蓋 48%，其餘
  積木的參數名沒有人守」那個洞。
- manifest 的 `default`：文字類參數一律給樣本值（`連接 (你好) 和 (世界)`）。
  `data.get` 的文字改成 `取得 (名稱)`（§4.5）。

### 前端

- **點一下就跑**（`App.tsx`）：一個 Blockly `CLICK` listener，點到影子積木就往上
  找一顆真的。點欄位不會誤觸發——Blockly 的 gesture 對欄位發的是 `doFieldClick`，
  根本不發 CLICK。與綠旗共用同一條路（存檔 → `POST /api/runs` → WebSocket），
  差別只有多送一個 `blockId`。
- **落單堆疊的 warning icon**（`blockly/inert.ts`）。文案順帶把「點它就能單獨
  執行」講出來——使用者第一次遇到落單堆疊，正是他剛拉出幾顆積木想試試看的時候。
- 工具箱換成 `@blockly/continuous-toolbox`：一條連續的捲動軸，點分類是捲到那一
  段。它順帶解掉「固定寬度」——flyout 只有一份，寬度不再隨分類跳動。
- 值氣泡改對齊積木**中央**（實測 816.077 對 816.077）、變數面板可關（開關存
  `localStorage`，`prefs.ts`，**不進 IR**）、左下角補上擴充功能入口（Scratch 放
  「添加擴展」的位置，現在列出已載入的 10 個命名空間）。
- `vite.config.ts` 認 `BLOCKY_BACKEND`：可以同時開兩份，一份是自己在用的編輯器，
  一份是正在改的那份。驗證一個改動不必先把自己那份關掉。

**做的過程中發現的三件事**

| 發現 | 為什麼要記 |
|---|---|
| **`style: { hat: 'cap' }` 只有第一顆積木有效。** Blockly 的 `jsonInit` 讀完會把定義物件上的 `style` 設成 `null`，而那份定義物件是所有同型別積木**共用的同一個** | 原本記的症狀是「帽子的弧度比 Scratch 平得多」，實際上是**根本沒畫帽子**——工具箱先生一顆，畫布上那顆就沒有了。差點就去調 `START_HAT_HEIGHT` 的幾何，而 zelos 的幾何本來就是 Scratch 的那條曲線。改用 extension（每顆積木的 init 都跑）之後一次就對。教訓：症狀是「偶爾才對」的時候，先問**誰先被建立** |
| **`setWarningText(null)` 不帶 id 是「拆掉整顆警告圖示」**，不是「清掉我那一則」 | 存檔前清警告的那一行（第 5 步寫的）會順手清掉落單堆疊的 ⚠。症狀是存一次檔畫布上的警告全部消失，直到下一次改動才回來。兩則警告從此各有各的 id |
| **AST 體檢跑完 87 顆積木，零不一致** | 本來預期會抓到幾個參數名對不上。沒抓到不代表白寫——它守的是**以後**：`t.value(b, "cond")` 對上 `args: {condition: …}` 這種漂移，改 handler 的當下看不出來，而題庫只覆蓋一半的積木。順帶記一條寫法：跨函式追要**帶上呼叫端的實參**（`_require_ts(t, b, "a")` 的 `"a"` 蓋過參數預設值 `"time"`），少了這一半測試自己會產生假警報，而假警報最後一定會被人關掉 |

**設計文件補了 v0.6**：§5.1 補上「從堆疊頂端起跑」「起點在建立 Run 之前解析」
「`trigger` 記為 `manual`」三條；§7.2 新增一節說明 `default` 是編輯器的影子值，
以及**通用值孔為什麼仍然留空**（Q16 的具體代價：`default: 0` 會存成字串 `"0"`，
於是 `如果 count = 0` 為假，而那是使用者永遠查不出來的一個錯）。

## 2. 未解決問題與已知限制

- **點一下就跑 = 每次都先存檔**（PUT + POST）。沒有節流，快速連點就是好幾次
  存檔加好幾個 Run（前一個會被停掉，所以不會累積，但存檔請求會）。單機沒有
  痛感，接上遠端後端時要重看。
- **落單堆疊的警告每次工作區事件都全掃一遍頂層積木**。幾百顆積木的專案還好
  （只掃 top blocks，不掃全部），但它是「拖曳中每個 move 事件都跑一次」的東西，
  §8.5 的靜態檢查（第 6 步）如果也這樣接，兩個加起來就要重想。
- **`var.set` 的窗口內收斂**（同名只留最後一次，§6.2）比設計文件原本寫的多。
  之後要做「變數變化的歷史曲線」時，這條收斂是第一個要重新談的東西。
- **§6.3 的 SQLite 落地沒做**，執行歷史只在記憶體、上限 50 個 Run，重啟就沒了。
  `GET /api/runs/{id}/events`（附錄 A）因此還不存在。`persist_*` 同樣跨 Run 但
  **不跨後端重啟**——D12 明講要有的性質，目前是欠的。
- **§5.5 的「清理有 5 秒上限」沒實作**。停止時只是 `cancel()`。P0b 只有內建積木、
  沒有東西要清，但 P1 接上 `ctx.http` 之後這條就會咬人。
- **`block.error` 沒有 traceback**：`BlockyError.to_dict()` 只有 type/code/
  message/blockId/hint，所以 §8.3 說的「點擊展開 traceback」目前只展得出 hint。
  完整 traceback 要跟 §6.3 的落地一起做。
- **非字串字面值打不出來（§16 Q16）**：`data.set`、`operator.eq` 這類宣告成通用
  `type: string` 的孔，編輯器只能透過文字框打字，而文字框只產得出字串——打 `99`
  拿到 `"99"`，布林與 `null` 更是沒有入口。這一步**刻意沒有用 `default` 繞過**
  （寫 `default: 0` 會存成 `"0"`，比空著更糟：它看起來是對的）。判定跟第 6 步
  一起做，理由見 §3。
- **變數面板的開關存 localStorage**，§16 Q15（偏好放哪）仍未定案。出入口只有
  `prefs.ts` 的兩個函式，換掉時要改的地方就這麼多。
- **P1 剩下的部分刻意延後**：SubprocessHost 與跨 process 的反向通道（§7.6）、
  `ctx.http`（§7.4）、secret 值遮蔽（§12.2）、migrations（§13.2）。介面已定案、
  合約測試已對 host 參數化，SubprocessHost 接上去只要在 `HOSTS` 加一行。
- **§8.4 完成了轉換，但沒有互動編輯**：函式的 mutator 對話框、變形失敗後的孤兒
  處理、靜態警告全部還是第 7 步的事。`procedures.ts` 的每函式一組類型是撐到
  第 7 步的過渡機制，不是最終形態。
- **變數索引 `variables` 目前一律存空物件**。§4.5 說這欄位是衍生索引，刪掉重算
  不影響執行語意；變數監看面板現在的來源是執行中的 `var.set` 事件，不是這個
  索引，所以它繼續留空仍然是誠實的。
- **`ui.multiline` 的第三層（強制切換）還沒接上序列化**。`FieldText` 已經有
  `setForcedMultiline`（第 3 步），但 `deserialize.ts`/`serialize.ts` 都還沒讀寫
  `blocks[].ui.multiline`——右鍵選單本身也還沒做（第 6 步）。
- **動態下拉（`source`）還是文字框**。要 `POST /api/extensions/{id}/dropdown/{source}`
  （§8.1 第 4 步，可能跟第 6 步一起）。
- **空的變數名稱欄位很難發現**。§8.5 的 autocomplete（第 6 步）會解決。這也是
  這一步**沒有**替變數名稱補 `default` 的原因：塞一個假名字進去不是解法。
- **題庫覆蓋仍是 42/87（48%）**，這一步的 5 題用的都是既有 opcode。差別是那 45 顆
  沒有題目的積木，**參數名現在有 AST 測試守著**了；缺的是「它真的跑得動」那半。
  `data.list_insert` 用 `len+1` 正規化索引，仍**無測試**，可疑。
- **§17.2 有幾列還寫不出題目**：`concurrency` 的 drop/queue/restart、
  `CancelledError` 穿透、§6.3 的 SQLite 落地——都要等 P2 的機制存在。
- **`blocky serve` 沒有正式打包測試**：`[project.scripts]` 加了，但只驗過
  `python -m blocky.cli`，沒驗過 `pip install` 之後的 `blocky` 指令。
- **遞迴 headroom 是估的**：`PYTHON_FRAMES_PER_BLOCKY_FRAME = 24`
  （`interpreter/engine.py`）為經驗值，靠 `RecursionError` 保險絲兜底。
- **Q10 目標使用者未定**（教育 vs 開發者）。不阻擋 P0b；P1 的三個手寫包開工前必須定。
- 設計文件 §16 的 Q1、Q3–Q9、Q11、Q12、Q14–Q16 仍未決。

## 3. 下一次的第一個 TODO

**P0b 第 6 步：`FieldText`（§8.5）**（估 1～1.5 週）——`${}` pill、autocomplete、
多行、運算式紅線。**這是整個 P0b 唯一的高風險項**，設計文件把原估的 2～3 天明確
上修成 1～1.5 週，理由是 Blockly 的自訂 field API 在富渲染上很難纏（popup 在
textarea 內的定位、workspace 縮放時的座標換算）。**一次做成一個類別**，用 options
開關能力——設計文件連續兩處警告不要先做三個再合併（§8.5、§15 施工順序），第 3 步
已經照這條留好了介面，第 6 步只換實作。完成驗收 3 與 4。

前面幾步留的兩個著力點，第 6 步都用得上：

- **值氣泡的定位**（`RunBubbles.tsx` 的 rAF 迴圈）就是 autocomplete popup 會遇到
  的同一個問題——在縮放中的 workspace 上把 HTML 疊到一個 SVG 元素旁邊。那裡已經
  有一個可以照抄的答案（現在還多了「對齊中央」那半）。
- **右鍵選單**要一次服務三件事：Q16 的字面值型別切換、`ui.multiline` 的強制切換、
  §4.5 的「重新命名所有引用」。三件事都在等同一個選單，所以它不是第 6 步的附屬品
  而是它的一部分。

**同時要決的是 §16 Q16（非字串字面值）**。傾向解 (a)「影子積木依當下的值選型別 +
右鍵切換」，而右鍵選單與欄位行為正是第 6 步要重寫的東西——現在做等於在一個即將
被換掉的欄位上加功能。候選解 (c)（manifest 的 `literalKinds`）不依賴 `FieldText`，
但 (a) 沒做之前沒有人讀它，先宣告只是把猜測寫進 manifest。

**時程**：第 5b 步花掉的就是估的 3～5 天，P0b 仍在 **5～7 週**這條線上。
